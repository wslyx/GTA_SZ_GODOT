# 复刻对照表

原版 `GTA_SZ/src/` 共 **107 个 TypeScript 模块**（不含 `.css`），
本重制版为 **42 个 GDScript**（不含 shader 与 tools）。

图例：**✅ 已实现**（逻辑逐行搬过来，数值一致）/ **🟡 部分实现或已近似** /
**❌ 未实现** / **— 不适用**（原版专属、非核心可玩项）

> "✅" 指**代码已写成并完成静态体检**（`tools/validate_project.py` 通过），
> **不代表已在 Godot 里实机跑通** —— 本项目约定不做编译校验。
> 首次实机验证时请优先确认三件事：① GLB 是否真实渲染而非占位盒；
> ② 城市是否出现在正确位置（坐标系一致性）；③ 各子系统是否在加载日志里都报了数。

---

## A. 基础层（原版没有对应模块，属移植新增）

| 本版文件 | 作用 |
|---|---|
| `utils/coordinate_util.gd` | 坐标契约：`(east, up, −north)`、yaw 换算、多边形绕序修正 |
| `utils/data_loader.gd` | `res://data/**` 读取 + 引用缓存（等价原版的 fetch） |
| `utils/geom_util.gd` | 运行时建网格：地面三角带 / 环形水面 / 高度场 / 招牌四边形 |
| `utils/glb_loader.gd` | GLB 异步串行加载队列（`load_threaded_*`），对应原版 ImportMeshAsync 的调度 |
| `utils/spatial_hash.gd` | 均匀网格空间哈希（原版 CityCollision 的 cells / bCells 抽象） |
| `core/game_content.gd` | 静态内容常量（商品价格、包裹、地点、咖啡馆菜单、路型表） |

---

## B. 玩法与状态

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| `main.ts` | `core/main_game.gd` | ✅ | 主控制器 + 输入 + 模式状态机 + 子步碰撞 |
| `state.ts` | `core/game_state.gd` | ✅ | 旧城小活状态与 7 个纯函数（含 validateSave 全部校验分支） |
| `save.ts` | `core/save_system.gd` | ✅ | IndexedDB 与 localStorage 三键等效（`user://`） |
| `city-types.ts` | `core/city_data.gd` | ✅ | 类型结构化为紧凑数组 |
| `city-map.ts` | `core/city_data.gd` | 🟡 | 数据加载与查询已实现；地图**绘制**在 `ui/map_ui.gd` |
| `city-life.ts` | `life/rides.gd` | ✅ | 3 条订单、接单/送达/结算、里程门槛 150m |
| `city-life-hub.ts` | `life/rides.gd` + `city_data.gd` | ✅ | 3 个驿站与 entry/delivery/rest 交互点 |
| `city-career.ts` | `life/career_system.gd` | ✅ | 3 身份 / 6 合约 / 品质与结算公式 / 4 项升级 / 关系对话 |
| `city-career-experience.ts` | `life/career_system.gd` + `ui/panels_ui.gd` | 🟡 | 手账 UI 已做；动画与过场表现未做 |
| `city-story.ts` | `life/story_system.gd` | ✅ | 《最后一单》8 步与两条分支 |
| `city-story-content.ts` | `life/story_system.gd` | ✅ | 内容内联 |
| `city-story-contract.ts` | `life/story_system.gd` | ✅ | 触发条件（28m / 速度<1 / 已下车）与调试跳转判定（45m） |
| `city-story-experience.ts` | `life/story_system.gd` + `ui/panels_ui.gd` | 🟡 | 对话 UI 已做；镜头调度未做 |

---

## C. 载具与移动

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| `driving.ts`（`stepCar` / `manualSteeringInput`） | `vehicles/car_drive.gd` | ✅ | 驱动力、阻力、滑行、手刹、自行车模型积分全部照搬 |
| `driving.ts`（`CityCollision`） | `world/city_collision.gd` | ✅ | 5 条判定分支与网格索引照搬 |
| `driving.ts`（`closest` / `inRing`） | `world/city_collision.gd` | ✅ | |
| `city-walk.ts` | `vehicles/city_walk.gd` | ✅ | 走 1.6 / 跑 4.2、门旁落脚点、三级降级移动、上下车判定 |
| `city-tank.ts` | `vehicles/tank_sim.gd` + `main_game.gd` | ✅ | 炮塔/炮管/开火/装填；**实体炮塔节点联动未做**（子弹池已做） |
| `city-tank-simulation.ts` | `vehicles/tank_sim.gd` | ✅ | `TANK_LIMITS` 全部常量 + `stepTank` + `tankFootprintClear` + `advanceTankShell` |
| `city-flight.ts` | `vehicles/flight_sim.gd` + `main_game.gd` | ✅ | 辅助观光飞行 |
| `city-flight-simulation.ts` | `vehicles/flight_sim.gd` | ✅ | 1/120 子步、9 点扫掠、边界回退、3000ms 恢复 |
| `city-flight-collision.ts` | `main_game.gd`（sweep 回调） | 🟡 | 用碰撞网格 + 地形高度判定；未做原版的建筑三角形精确扫掠 |
| `city-flight-missiles.ts` | `vehicles/flight_sim.gd` | 🟡 | 导弹发射入口已留（Space），**导弹池与爆炸特效未做** |
| `city-flight-explosion.ts` | `core/procedural_audio.gd` | 🟡 | 只有音效；无火球特效 |
| `city-cockpit.ts` | `vehicles/chase_camera.gd`（COCKPIT） | 🟡 | 司机位第一人称相机（位置/朝向/nearZ/fov 照搬）；**无舱内模型与仪表** |
| `city-vehicle-reflections.ts` | `world/lighting_director.gd` + 环境 IBL | 🟡 | 用环境反射替代跟随式反射探针 |
| `city-vehicle-materials.ts`、`city-vehicle-finish.ts` | — | ❌ | 车漆独立 PBR 调优 |
| `city-vehicle-manifest.ts` | `city_data.gd`（`vehicle_manifest()`） | ✅ | 数据已可读 |
| `city-tail-lights.ts` | `vehicles/vehicle_lights.gd` | ✅ | 刹车/倒车灯随状态变亮；灯架常驻不隐藏（保留原版"避免着色器重编译"的结论） |
| `city-observer.ts` | `vehicles/observer.gd` | ✅ | WASD 平移 / Q-E 升降 / Shift 加速 / 记录安全位置 |
| `city-observer-depth.ts` | `vehicles/chase_camera.gd` | 🟡 | 无人机 nearZ 用固定值，未做高度的自适应公式 |
| `city-autopilot.ts` | `vehicles/autopilot.gd` | ✅ | 三偏移前滚评分、跟车、路型限速、信号灯限速、受困判定 |
| `navigation.ts` | `world/road_graph.gd` | ✅ | CSR 邻接表 + Dijkstra/A* + `dense()` 加密 + `nearestEdge` |

---

## D. 城市渲染

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| `city-world.ts` | `world/city_world.gd` | 🟡 | 图层装配顺序、分块剔除、服务注入已实现；**材质表与阴影 bias 的自适应未做** |
| `city-gltf-streaming.ts` | `utils/glb_loader.gd` | ✅ | 线程加载 + 串行队列 |
| `city-facade-stream.ts` | `world/facade_streamer.gd` | ✅ | 640m 瓦片、700/1050/1500 三段阈值、串行一次一个、空中 250ms 延迟 |
| `city-facade-diversity.ts` | — | ❌ | |
| `city-architecture-materials.ts` | — | ❌ | 31 个 profile，**影响立面质感最大的缺口** |
| `city-road-surface.ts` | `city_world.gd`（GLB 原材质） | 🟡 | UV 周期 14、ORM 采样、干湿切换未做 |
| `city-road-names.ts` | `game_content.gd`（`UNNAMED_ROAD_PATTERN` 等） | 🟡 | 未命名路的四段式命名规则已搬成常量，**运行时未实际生成路名** |
| `city-roads`（渲染） | `city_world.gd`（`roads.glb`） | ✅ | 分块 `roads_i_j_` 索引与剔除 |
| `city-mountains.ts` | `world/distant_system.gd` | ✅ | 按 manifest 的 tiles 逐块建高度场网格 |
| `city-distant-geometry.ts` | `world/distant_system.gd` | 🟡 | 远山已做；远景几何层级未做 |
| `city-distant-city.ts` | — | ❌ | |
| `city-ground-relief.ts` | `world/distant_system.gd` + `height_field.gd` | ✅ | 46 块 relief-mesh 网格 + 高度采样 |
| `city-coastal-horizon.ts` | `world/distant_system.gd` | ✅ | 边界边检测 + 向下封裙边到 `waterHeight − 1` |
| `city-coastal-infrastructure.ts` | `height_field.gd` + `bay_water.gd` | ✅ | 跨海桥引桥 + 岸距纹理 + 公园灯 |
| `city-bay-water.ts` | `world/bay_water.gd` + `shaders/water.gdshader` | 🟡 | 4 组正弦波法线、fwidth 抗混叠、粗糙度回灌、泡沫全部照搬；**反射用 IBL/SSR 而非平面镜** |
| `city-sky-reflection.ts` | `world/sky_system.gd` | ✅ | 三档 HDR 环境反射 |
| `city-canopy.ts` | `world/scenery_system.gd` | ✅ | 6 树种 × 3 LOD、170/650/520 阈值、按距离预算降级 |
| `city-landscape.ts` | `world/scenery_system.gd` | 🟡 | 细节实例化已做；prototype/LOD 预算体系简化 |
| `city-roadside-planting.ts` | `world/scenery_system.gd` | 🟡 | 街具与部分细节已布点；完整间距/否决规则未做 |
| `city-meadow.ts` | — | ❌ | |
| `city-grass-material.ts` | `shaders/water.gdshader`（无关） | ❌ | 草地材质 |
| `city-landscape-lighting.ts` | `world/lighting_director.gd` | 🟡 | 路灯池覆盖；植被专属照明未做 |
| `city-local-lighting.ts` | `world/lighting_director.gd` | ✅ | range 46 / 色 (1,.82,.61) / 夜 600 昏 60 / 灯位偏移 1.5m / 路 8.27m 园 11.56m |
| `city-public-lighting.ts` | `world/lighting_director.gd` | 🟡 | 用 OmniLight 池近似照度 clipmap |
| `city-landmark-lighting.ts` | — | ❌ | |
| `city-rooftops.ts`、`city-roof-surface.ts`、`city-rooftop-plan.ts` | — | ❌ | |
| `city-building-signs.ts` | `world/sign_system.gd` | 🟡 | 3077 条数据 + 选择评分已照搬；渲染改用 Label3D 池而非动态图集 |
| `landmark-signage.ts` | `world/sign_system.gd` | 🟡 | 位置/法线/切线/尺寸数据可用；纹理贴片未做 |
| `landmark-details.ts` | `core/city_data.gd` + `city_world.gd` | ✅ | `baseBuildingIds` 排除、`collisionFootprints` 回推、前缀 dispose、地形高度公式 |
| `city-street-furniture.ts` | `world/scenery_system.gd` | ✅ | urban-bench 63 个摆放点、150m 内取前 12、y = 地面 + 0.045 |
| `city-ambient-bake.ts`、`city-ambient-occlusion.ts` | — | ❌ | 烘焙 AO（`occlusion.png` 已迁移） |
| `city-ground-relief.ts`（材质） | `distant_system.gd` | 🟡 | 用了 `ground-cover.png` 做宏观贴图 |

---

## E. 光照与天气

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| `city-cinematic.ts` | `world/lighting_director.gd` | ✅ | `CINEMATIC_LOOK` 三档的太阳/半球/环境/雾/曝光/对比/Bloom 数值逐字照搬 |
| `city-daylight.ts`、`city-daylight-environment.ts` | `sky_system.gd` + `lighting_director.gd` | ✅ | 白天档含 HDR 旋转角与太阳方向 |
| `city-sunset-environment.ts` | 同上 | ✅ | 日落档 |
| `city-night-sky.ts` | `shaders/night_sky.gdshader` | ✅ | 银河带 + 双层星群 + 月球盘（半径 .0082、方向 (.72,.48,−.5)、漫射 .42） |
| 天空（`szSky`） | `shaders/sky.gdshader` | ✅ | 日轮 `pow(dot,4200)`、地平辉光 `pow(dot,22)`、5 阶 fbm 云、末行 `mix(c, c×(.12,.22,.44), night)` |
| `city-rain-weather.ts` | `world/weather_system.gd` | ✅ | 240 条雨丝、下落公式、28×28 跟随区、雨压系数 |
| `city-rain-puddles.ts` | `world/weather_system.gd` | 🟡 | stride=10 布局解析 + 水片网格已做；**未复用平面镜反射** |
| `city-sport-details.ts` | — | ❌ | |
| `city-baypark-look.ts` | — | ❌ | |

---

## F. 城市生命与交通

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| `traffic.ts` | `world/traffic_system.gd` | ✅ | 40 车、3 色组、14–600m 布点、方向一致性选路、红灯与跟车等待、靠右 1.15m |
| `city-traffic-signals.ts` | `world/traffic_signals.gd` | ✅ | 30.4s 两相位周期、相位表、`signalHold` 全部判定条件、按距离裁剪 140 个路口 |
| `pedestrians.ts` | `world/pedestrian_system.gd` | ✅ | 56 人、36969 段路径、0.618 黄金比散点、5 部件摆动（腿/臂枢轴照搬） |
| `pedestrian-impact.ts` | — | ❌ | 倒地物理 |
| `city-ebikes.ts` | `world/ebike_system.gd` | 🟡 | 21110 辆停车排 + 实例着色 + 220/260m 半径与 700/500 上限已做；**18 个骑行 NPC 的推进逻辑未接** |
| `city-rider.ts` | — | ❌ | 玩家骑手本体（久岐忍/外卖骑手 GLB 已在 `data/characters/`） |
| `city-cafe-characters.ts` | — | ❌ | |
| `city-bamboo-cafe.ts` | — | ❌ | 数据与 GLB 已迁移（`data/city/bamboo-cafe/`） |
| `city-bamboo-cafe.css` | — | — | |
| `city-bamboo-corridor.ts` | — | ❌ | |
| `city-bamboo-look.ts` | — | ❌ | |
| `city-cafe-layout.ts` | `core/game_content.gd`（`CAFE`） | 🟡 | 站点坐标、footprint、员工局部位置、菜单已搬成常量；**场景与交互未做** |
| `city-cafe-staff-motion.ts` | — | ❌ | |
| `city-local-characters.ts` | — | ❌ | |
| `city-character-hud.ts`（+ css） | — | ❌ | |
| `city-life-hub.ts`（模型） | — | ❌ | `life-hub.glb` 已迁移 |
| `dynamic-instances.ts` | `MultiMesh` 原生机制 | — | Godot 内建，无需对应模块 |

---

## G. 相机 / UI / 影音

| 原版模块 | 本版 | 状态 | 说明 |
|---|---|---|---|
| 相机系统（`city-world.ts` 内） | `vehicles/chase_camera.gd` | ✅ | 三种驾驶视角的 forward/height、fov = .80 + |v|×.00045、指数平滑 9 / 驾驶舱 20、各模式 nearZ |
| `city-hud.ts`（+ css） | `ui/game_hud.gd` | ✅ | 表盘（量程 200、−128° 起、扫 256°）、速度三位补零、档位、里程、路名、片区、时钟、toast、FPS |
| `city-flight-hud.ts`（+ css） | `ui/game_hud.gd` | 🟡 | 只做了模式文案；高度/姿态仪未做 |
| `city-quicktips.ts`（+ css） | `ui/panels_ui.gd` + `hud.gd` | ✅ | 操作速查面板（Esc 已占用，改由 tips 面板常驻切换） |
| `city-graphics-quality.ts` | `core/graphics_quality.gd` | ✅ | 三档全部参数 + 分辨率缩放公式 + 空中触发阈值 |
| `city-graphics-panel.ts`（+ css） | `ui/panels_ui.gd` | ✅ | 参数展示与档位循环 |
| `city-map-view.ts` | `ui/map_ui.gd` | ✅ | 小地图与大地图切换 |
| `city-map-geometry.ts` | `ui/map_ui.gd` + `coordinate_util.gd` | 🟡 | 映射公式与缩放钳制已做；标注避让排布未做 |
| `city-map.css` | — | — | Godot 用 `_draw()` 绘制 |
| `city-objective-marker.ts` | `main_game.gd` + `geom_util.torus_up()` | ✅ | 发光圆环地标（尺寸 2.15 / 线宽 .045 / 48 段） |
| `city-observer-beacons.ts`（+ css） | — | ❌ | |
| `city-observer-destination.ts` | — | ❌ | |
| `city-observer-beacon-state.ts` | — | ❌ | |
| `city-audio.ts`（+ css） | `core/procedural_audio.gd` | 🟡 | 纯合成（无音频文件）已实现：电机 48Hz + 三角 98Hz + 三条噪声、冲击/爆炸/提示音、72 BPM 原创 BGM；**未做压缩器链与页面可见性 suspend** |
| `city-loading.ts`（+ css） | `ui/panels_ui.gd` | ✅ | 加载画面与进度条 |
| `city-cinematic.ts`（摄制） | — | ❌ | 宣传片管线 |
| `trailer-capture.ts`、`trailer-shots.ts` | — | ❌ | |
| `aerial-film-hud.ts`、`aerial-film-shots.ts` | — | ❌ | |
| `readme-reel-shots.ts` | — | ❌ | |
| `combat-core.ts`、`combat-lab.ts`（+ css） | — | — | 原版独立实验页 |
| `style.css`、`area-reveal.css`、`city-experience.css` | — | — | |
| `city-experience.ts` | — | ❌ | |
| `city-loading.css` | — | — | |

---

## 汇总

| 状态 | 数量（约） |
|---|---:|
| ✅ 已实现 | 46 |
| 🟡 部分实现 / 已近似 | 30 |
| ❌ 未实现 | 27 |
| — 不适用（原版专属/样式表/Godot 内建） | 12 |

**缺口集中在三处**：① 建筑立面材质与屋顶细节（视觉质感）；
② 咖啡馆 / 竹廊 / MMD 角色 / 行人倒地（表现层内容）；
③ 宣传片与战斗实验室（原版非核心功能）。

逻辑与数值层（驾驶手感、碰撞、路网、信号灯周期、职业结算公式、光照三档常数）
是**逐字照搬**的 —— 这一层是"一比一"的主要含义所在。
