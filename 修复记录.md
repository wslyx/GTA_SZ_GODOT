# 深城纪 · Godot 重制版 缺陷审查与修复记录

审查日期：2026-09-21　引擎：Godot 4.7.1 stable　方式：静态审查 + Godot 实机运行验证

性能优化：2026-09-22　方式：热点勘察 + Godot 4.7.1 无头编译校验 + 焦点驱动实测

地图/自动驾驶修复：2026-09-23　方式：静态审查 + 离线数据复算（navigation.json 42829 节点真机路网）

远景纯色幕墙修复：2026-09-23　方式：实机截图取色 + 离线几何复算（天空球交点）＋ Godot 4.7.1 无头解析校验

大地图鼠标穿透修复：2026-09-23　方式：最小工程真机复现（Godot 4.7.1 合成事件）＋ 端到端合成事件验证

大地图文字乱码修复：2026-09-23　方式：实机截图逐区域放大比对 + 最小字体对照实验

过桥穿模修复：2026-09-23　方式：离线复刻原版 TS 算法差分比对（30768 采样点）＋ 端到端车身高度实测

画面一比一对齐：2026-09-24　方式：实机批量截帧 + 分区像素量化比对（原版截图 1920×1124 vs 同尺寸复刻帧）

启动加载画面 / 起始点画面复刻：2026-09-24　方式：原版 CSS 逐值换算 + 原版截图分区放大比对（未做编译校验，见文末）

帧率 40 → 满帧：2026-09-24　方式：实机探针（tools/_perf.gd）配置矩阵 + 多轮对照归因 + 行驶 600 帧验证

加载 3D 动画补齐：2026-09-24　方式：原版 mp4 同码率转 Theora + VideoStreamPlayer 接线（层级/淡入/释放对齐原版）

下车步行角色与人称切换：2026-09-24　方式：原版 city-rider.ts 逐条移植 + walkshot 探针整链截图验证（F→走→C→F）

---

## 十七、下车没有人物模型 / 人称切换异常（P1，步行体验）

> 2026-09-24。用户反馈"下车后没有人物模型、切换人称也有问题"。
> 排查结论：**步行相机与 C 键人称管线本来就在**（`_step_walk` →
> `camera.follow_walk(dt, distance, first_person)`），缺的是原版
> `city-rider.ts` 的角色本体 —— 对照表里正好是那一行 ❌。

### 17.1 原版行为（city-rider.ts + city-world.ts:500/530）

- 角色：本地角色清单里的 **久岐忍**（`data/characters/kuki.glb`，displayHeight
  1.72，自带 `Rider_Idle / Rider_Walk / Rider_Run` 三段烘焙步态动画）；
- 锚点随步行位置摆放，**yaw 平滑跟随移动方向**（`riderYaw += Δ·(1−e^(−dt·16))`），
  视线方向由相机单独处理；
- 步态按速度加权混合，并用「一个步态周期的步幅」把播放速度锁到实际移速
  （`phaseClock.speedRatio = speed / 步幅米数`），脚底不打滑；
- 第一人称时整个模型隐藏：`setEnabled(walk.active && (!firstPerson))`；
- F 下车需要角色就绪，toast `${name} · WASD 行走 / Shift 跑步 / 拖动环视 /
  滚轮远近 / F 上车`；F 上车 toast `已上车 · C 切换驾驶镜头`。

### 17.2 复刻实现

- **新增 `scripts/vehicles/rider.gd`**（Rider）：载入 kuki.glb，找 AnimationPlayer，
  按 GLB 里的 `Rider_Idle/Walk/Run` 选段。原版是三段**连续权重混合**；Godot 的
  AnimationPlayer 一次只播一个动作，这里用「按速度选段 + 0.25s 交叉淡入 +
  `speed_scale = 实际移速 / 该段标定步幅速度`」近似原版的相位锁
  （标定值取 `data/characters/manifest.json` 的 gait：walk 1.032m/cycle、
  run 1.9m/cycle，分档阈值取原版 running ramp 的中点 2.9m/s）；
- `main_game.gd`：`_load_vehicle_models()` 里同步预载（12MB GLB 的实例化成本
  藏进启动加载，首次按 F 不卡顿）；下车 `_rider_yaw = walk.yaw`；每帧
  `set_enabled(mode==WALKING && !first_person)` + `set_pose(...)`；
- C 键补 toast（`步行 · 第一人称 / 步行 · 第三人称`），F 的 toast 文案对齐原版；
- ⚠️ 新脚本的 `class_name` 要等编辑器重扫才会进全局类缓存，运行时直接跑会报
  `Could not find type "Rider"` —— 所以 main_game 用 `preload` 引用（项目既有做法）。

### 17.3 顺带修复：快捷键条不随模式切换（P2）＋「? 操作」按钮显示错键（P2）

- 复刻版底部快捷键条固定显示驾驶条目（步行时还写着"坦克/手刹/下车"）。
  已把原版 `city-quicktips.ts` 的 **QUICK 四模式表**（driving / walking /
  observer / tank）搬进 `game_hud.gd`，随模式切换键位与左端模式名
  （`步行` / `观景` / `坦克`），飞行中整条隐藏（与原版一致）；
- 顺带发现遗留 bug：`_build_quicktips` 建了「?」方块却**没 append 进
  `_tip_boxes`**，而 `_draw_quicktips` 把列表最后一项当「? 操作」切换钮 ——
  于是切换钮一直显示最后一个条目的键（驾驶模式显示 `[Y]`）。已补 append。

### 17.4 验证（tools/_perf.gd 新增 `walkshot` 模式，整链截图）

F 下车 → 第三人称立绘（久岐忍背对镜头、车已隐藏、toast 对）→ 按住 W 行走
（Walk 动画、006 KM/H ≈ 步速 1.6m/s）→ C 第一人称（模型隐藏、toast
「步行 · 第一人称」）→ C 回第三人称 → F 上车（走远后被正确拒绝：
「走近车门再按 F 上车」）。快捷键条：步行显示 `步行 | WASD 行走 | 滚轮 远近 |
C 人称 | Shift 跑步 | 拖动 看向 | F 上车 | E 互动 | ? 操作`，驾驶显示
`驾驶 | WASD 驾驶 | T 坦克 | 空格 手刹 | C 镜头 | F 下车 | G 观景 | Y 雨天 | ? 操作`。
全程 0 脚本错误。

### 17.5 涉及文件

| 文件 | 改动 |
|---|---|
| `scripts/vehicles/rider.gd` | **新增**：步行角色本体（载模/选段/锁步/显隐） |
| `scripts/core/main_game.gd` | 预载角色、下车/上车接线、每帧 set_pose、C/F toast |
| `scripts/ui/game_hud.gd` | QUICK 四模式表随模式切换；修复「? 操作」切换钮缺 append |
| `tools/_perf.gd` | 新增 `walkshot` 模式（下车链路整链截图） |

### 17.6 复测补充：走路动画播一遍就冻住（P1，回归）

> 用户复测：走路动画只在刚开始有，速度到 6km/h 腿就不动了。

**根因**：Godot 导入的 GLB 动画默认**不循环**。Walk 段长 1s、播放速率
1.55×（移速 1.6m/s ÷ 步幅速度 1.032m/s）≈ 0.65s 播完 —— 此刻速度表正好爬到
6km/h，之后人物冻在最后一帧，与"刚开始有、6km/h 就停"完全吻合。原版是
`group.start(true)` 循环 + `ANIMATIONLOOPMODE_CYCLE`。

**修复**（`rider.gd` `setup()`）：对 idle/walk/run 三段显式设
`loop_mode = Animation.LOOP_LINEAR`。

**复验**（walkshot 隔 1.6s 抓两张行走帧）：腿部区域平均像素差 13.67
（静止/冻结应≈0），并排目检两帧处于不同步态相位（左：右腿前迈左脚跟抬起；
右：双腿收拢换步）—— 循环生效。


---

## 十六、启动加载画面的 3D 动画补齐（P1，画面）

> 2026-09-24。用户要求加载画面的"原版 3D 动画"也要一致。原版 `city-loading.ts`
> 的 `.city-loader-film` 是一段**预录影片**（`bamboo-clay-loop.mp4`，
> `bamboo-clay-source.json` 记载：`春笋 · 冷暖光影环绕`，720 帧 / 30fps / 24s，
> 用游戏几何离线渲染的陶土城相机环绕，无音轨）—— 原版注释写明用它"避免加载时
> 再渲染第二座城市"。

### 16.1 为什么之前没做：Godot 只认 Theora

Godot 4 原生视频解码只有 Ogg Theora（`.ogv`），mp4/h264 不支持；当时环境里也没有
ffmpeg。本次用 `npm install ffmpeg-static`（gyan.dev 6.1.1 构建，带 libtheora）解决：

```bash
ffmpeg -i bamboo-clay-loop.mp4 -c:v libtheora -b:v 2254k -pix_fmt yuv420p -an bamboo-clay-loop.ogv
```

产出 **6.8MB / 24s / 1920×1080 / 30fps / 2265kb/s**，与原版 mp4（6.77MB、2254kb/s）
同码率同规格。源 mp4 保留在 `data/city/loading/` 里作母带。

### 16.2 接线（逐条对应原版行为）

| 原版 | 复刻 |
|---|---|
| `<video>` 盖在海报上、vignette 之下 | `VideoStreamPlayer` 插在 `_l_poster` 与 `_l_vignette` 之间 |
| `muted` | `volume_db = -80`（源片本就无音轨） |
| `loop` | `loop = true` |
| `object-fit:cover; object-position:60% 50%` | 复用海报的 cover 矩形（源片与海报同为 16:9） |
| `opacity:0`，`data-film=playing` 时 0.8s 淡入 | `modulate.a` 0 → 1 的 0.8s Tween（`_film_fade`） |
| `syncFilm()` 播放 / `releaseFilm()` 释放 | `show_loading()` 里 `play()` + 淡入；`_on_loading_faded()`（0.7s 淡出结束后）`stop()` + 归零 |
| 解码失败退回海报 | 素材缺失时节点透明（海报自动兜底）；`FileAccess.file_exists` 判断而不是 `ResourceLoader.exists`（`.ogv` 不走导入缓存时后者恒 false），`VideoStreamTheora.file` 运行时用 FileAccess 直读 res:// 源文件 |

### 16.3 验证

- `loadshot` 抓 0.4 / 1.2 / 2.0 / 2.8 / 3.6s：画面是陶土春笋 + 城市环绕（与原版
  `bamboo-clay-loop` 同源），右侧纯影片区域相邻帧平均像素差 14.5 / 73.4 / 45.3
  （静止会是 0）—— **影片确实在播**；
- 播放影片后启动耗时 5341 → 5860ms（Theora 主线程解码的代价，原版浏览器同样要解码），
  影片在 0.7s 淡出结束即停，不影响运行期帧率（matrix 实测仍 239fps）。

### 16.4 涉及文件

| 文件 | 改动 |
|---|---|
| `data/city/loading/bamboo-clay-loop.ogv` | **新增**：原版 mp4 的 Theora 转码（6.8MB） |
| `scripts/ui/panels_ui.gd` | 加载画面新增影片层（`_l_film` / `_film_fade`），播放/释放时机对齐原版 |
| `tools/_perf.gd` | 探针恢复默认 `matrix` 模式 |

---

## 十五、帧率只有 40fps 左右（P0，性能）：真凶是小地图每 0.2s 重画一遍

> 2026-09-24。用户反馈"现在帧率只有 40 帧左右"。这台机器是 **RTX 4060 Laptop，
> 显示器 240Hz** —— 40fps 意味着帧成本在 ~25ms 一带，必须先量出这 25ms 花在哪，
> 而不是按"看起来很重"去改代码。

### 15.1 先量再说：给项目加了一个实机性能探针

**文件**：`tools/_perf.gd` + `tools/_perf.tscn`（**保留**，后续排查还能用）

```bash
Godot_v4.7.1-stable_win64_console.exe --path . --resolution 1920x1080 --always-on-top res://tools/_perf.tscn
```

五种模式（改 `tools/_perf.gd` 顶部的 `MODE`）：

| 模式 | 作用 |
|---|---|
| `matrix` | 逐项关掉一个开销源（SSAO/Bloom/阴影/FXAA/缩放/天空/水/路/地形/地标/区块/立面/路灯/植被/招牌/行人/车流/电摩/信号灯），量丢帧率与 draw call 差值 |
| `rounds` | 多轮对照（每轮 120 帧，vsync 开），量**长帧（>50ms）**次数与帧时长直方图 |
| `tree` | 遍历场景树，按父路径汇总 MeshInstance × surface 数与三角面，定位 draw call 来源 |
| `drive` | 合成按住 W 真开车，行驶中统计长帧 |
| `loadshot` | 按启动时刻抓图（含加载画面阶段），人工比对版式 |

两条**测量教训**（都踩过，写下来免得再踩）：

1. **vsync 必须保持开**。首轮把 vsync 关了想测"真实帧时间"，结果主线程与渲染线程
   解耦，`delta` 只反映主线程跑多快（2ms/帧、屏幕实际 40fps），所有配置差值全被吞掉。
   vsync 开着时 `delta` 就是真实呈现间隔，>16.7ms 的帧就是丢帧。
2. **窗口必须 `--always-on-top`**。探针窗口被 IDE 盖住时 DWM 会跳过呈现，
   delta 变成 2ms + 周期性 114ms 的假数据。另外 `Performance.TIME_PROCESS`
   与 delta 严重不符（报 117ms 而 delta 只有 16ms），不能当每帧 CPU 时间用。

### 15.2 排除法：先证伪三个"想当然"

| 实验 | 结果 | 结论 |
|---|---|---|
| matrix 逐项关 SSAO/Bloom/阴影/FXAA/缩放 0.5/天空/水/路/地形/地标/区块/立面/路灯/植被/招牌/行人/车流/电摩/信号灯 | 帧率 **全部在 48~53 不动**（连分辨率减半都没用） | 不是 GPU 填充率/几何/阴影/后处理瓶颈 |
| `tree` 统计 | 场景只有 **1479 个 MeshInstance / 121 个 MultiMesh / 1602 个 surface / 4.29M 三角面**（大头：roads 765 个节点 2.02M 面） | 不是 draw call 爆炸（引擎报的 17337 是含阴影等多 pass 的口径） |
| 静止 vs 行驶 | 主线程逻辑 ~4ms/帧 | 不是游戏脚本/逻辑瓶颈 |

### 15.3 决定性实验：多轮对照抓长帧

`rounds` 模式，vsync 开、相机静止于出生点，每轮 120 帧：

| 轮次 | 配置 | 帧均值 | >50ms 长帧 | >16.7ms 帧 |
|---|---|---|---:|---:|
| baseline | 原样 | 17.97ms | **8** | 16 |
| no-minimap | 隐藏小地图 | **10.40ms** | **0** | 1 |
| no-minimap-hud | 再藏 HUD | 10.17ms | 0 | 0 |
| baseline2 | 恢复 | 19.98ms | **9** | 20 |

长帧的形状：**每 ~0.2s 一次 90~148ms 的尖峰**，与 `map_ui.gd` 的
`REDRAW_INTERVAL = 0.2` 完全吻合；卡住时世界状态是 `待载=0 加载队列 8/8`
—— **不是流式加载**，就是小地图那一次重绘。

### 15.4 根因：小地图没有原版的瓦片缓存，每 0.2s 全量重画

**文件**：`scripts/ui/map_ui.gd`

原版 `city-hud.ts` 的 `getMapTile()` 把每块 512m 瓦片画一次进 256px 离屏 canvas
（LRU 96 块），之后小地图只 `drawImage`；复刻版声明了 `MAX_TILE_CACHE := 96`
却**没有实现缓存** —— `_plane_draw.queue_redraw()` 每 0.2s 触发一次
`_draw_minimap_plane()`，把视野内全部要素用 CanvasItem 命令重画：

- 水域 312 + 绿地 1703 个多边形每次重画要 **CPU 重新三角化**（`draw_colored_polygon`）；
- ~1600 条可见道路 × 2 层 ≈ **3200 条抗锯齿折线**每次重建几何；
- 合计单次重绘 **~100ms**，表现就是"帧率 40 左右 + 每 0.2s 卡一下"。

### 15.5 修复：静态图层烘一次，实时路径只剩一次 blit

按原版思路做整城一张静态贴图（`_bake_static_map()` / `_draw_static_map()`）：

- 烘焙分辨率 = **1:1 平面分辨率**（`MINIMAP_SCALE` px/m ≈ 2.6m/px），全城约
  5287×2108（外扩 64px，~45MB 显存），一次性画完水域/绿地/道路（约 100ms，
  发生在 `setup()` 里，此时还在加载画面后面，玩家看不见）；
- 实时路径 `_draw_minimap_plane()` 只剩：底色 rect + 一次 `draw_texture` +
  导航线（有路线时）—— 与原版"缓存瓦片 → drawImage"的层级完全一致；
- 路线（导航）保持实时绘制，因为它随目的地变化；
- 烘焙失败的兜底路径保留（`_draw_static_direct`，老的剔除直绘）；
- `REDRAW_INTERVAL` 0.2 → **0.12**（对齐原版 city-hud.ts 的 HUD tick），
  重绘便宜了，跟随更顺滑。

### 15.6 验证（实机，tools/_perf.gd）

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 静止（baseline 轮） | 帧均值 17.97ms，长帧 8/120 | **4.37ms，长帧 0/120** |
| 隐藏小地图 | 10.40ms（说明那 8ms 全是小地图） | 4.24ms（差值 0.1ms，开销≈0） |
| 恢复（baseline2 轮） | 19.98ms，长帧 9/120 | 4.26ms，长帧 0/120 |
| **行驶 600 帧**（0→21m/s，触发区块加载） | — | **帧均值 4.23ms，最大 7.83ms，长帧 0，600/600 帧 <16.7ms** |
| 引擎 FPS 计数 | ~40–56 | **236~240（吃满 240Hz 刷新率）** |

画面核验（`loadshot` / `mapshot` 抓图，`tools/shots/`）：
小地图的水域/绿地/道路双层描边/车辆箭头/指北针/路名全部正常；大地图
（M，走 `_compute_bboxes` + `_road_boxes` 的另一条路径）路网、地标点、比例尺正常。

一个能解释一切的旁证：引擎的 `RENDER_TOTAL_DRAW_CALLS_IN_FRAME` 从 **17337 → 878**
—— 原先那 1.6 万次 draw call 里约 95% 是小地图平面里的 3200 条抗锯齿折线 +
2000 个多边形，而不是城市本身（城市本体只有 ~880）。所以这个 bug 同时是
**周期性 100ms 主线程卡顿**和**全项目最大的 draw call 来源**。

### 15.7 顺带修复：上一轮的加载画面暗角着色器从未编译通过（P1，画面）

`shaders/loading_vignette.gdshader` 用了 `const vec3 C1_0 := vec3(...)` ——
Godot 着色器语言没有 `:=`，启动即 `SHADER ERROR: Expected initialization of
constant`，材质回退成**纯白**：也就是说上一轮做完后加载画面其实是一块白板，
海报和暗角都没显示。改为 `const vec3 C1_0 = vec3(...)` 后正常（已用
`loadshot` 在 0.6~2.4s 抓图确认：海报、暗角、标题块、进度块全部按原版版式出现）。

### 15.8 涉及文件

| 文件 | 改动 |
|---|---|
| `tools/_perf.gd` / `_perf.tscn` | **新增**：实机性能探针（matrix / rounds / tree / drive / loadshot / mapshot 六种模式） |
| `scripts/ui/map_ui.gd` | 小地图静态图层烘焙（`_bake_static_map` / `_draw_static_map`），实时路径改为一次 blit；`REDRAW_INTERVAL` 0.2 → 0.12；兜底直绘保留 |
| `shaders/loading_vignette.gdshader` | `const vec3 X := ...` → `const vec3 X = ...`（上一轮遗留的编译错误） |

> 建议复测：跑一次游戏按 P 看帧率。240Hz 屏上应稳定在 200+；若仍有掉帧，
> 打开 `tools/_perf.gd` 用 `rounds` 模式看长帧直方图，即可定位。

---

## 十四、启动加载画面与起始点画面按原版复刻（P0，整体观感）

> 2026-09-24。用户给出两张原版截图（① 启动加载画面 ② 出生点的自由驾驶画面），
> 要求复刻版与原版保持一致。原版两份文件是唯一事实来源：
> `GTA_SZ/src/city-loading.ts` + `city-loading.css`，以及
> `city-hud.css` / `city-career.css` / `city-story.css` / `city-quicktips.css`。

### 14.1 加载画面：原版是「浅色海报 + 17 段文案」，复刻版原本是黑底居中两行字

**问题**（`scripts/ui/panels_ui.gd` 的 `_build_loading()`）：复刻版此前是一块
`Color(0.02,0.03,0.05)` 的纯黑屏 + 居中「深 城 纪 · SHENCHENGJI」+ 520×10 金色进度条，
**与原版完全不是同一个界面**。原版是：
浅色底 `#e8eae5`，上面盖一张 `bamboo-clay-poster.jpg`（陶土城模型，1920×1080）
按 `object-fit:cover; object-position:60% 50%` 铺满，再叠两层暖纸色 `linear-gradient`
暗角；文字全部是「深城纪（衬线 122px）+ 分隔线 + 把下班后的时间，还给这座城市。/ 南山 福田 罗湖」
＋ 底部 `城市骨架 / 正在展开深圳地图 / 64% / 进度条 / 阶段 12 / 17 / 按已完成阶段推进`。

**改动**（全部照 CSS 实测值，无一"看着差不多"）：

| 位置 | 原版数值 | 实现 |
|---|---|---|
| 海报 | `object-fit:cover; object-position:60% 50%` | `scale = max(vw/1920, vh/1080)`，偏移 `-(w−vw)×0.60 / -(h−vh)×0.50` |
| 暗角 | `linear-gradient(90deg,#f3efe6d9 0,#f1ede3a3 29%,#edf0e50a 60%,transparent 80%)` + `linear-gradient(0deg,#e9e6dccf 0,#e9e6dc66 17%,transparent 40%)` | 新增 `shaders/loading_vignette.gdshader`，两层按**直式 alpha 的 src-over**合成（`background: g1,g2` 里 g1 在上） |
| 顶栏 | `top 5.4% · left/right 6.1% · 10px · ls .23em · #526967` | 同左 |
| 标题块 | `left 11% · top 27%` | 同左 |
| h1 | 衬线 `clamp(65px,7.4vw,122px)` / `lh 1.3` / `ls .21em` / `#233b3e` | 字距走 `FontVariation.spacing_glyph`；`lh 1.3` 用半行距公式对齐基线 |
| 分隔线 | `42×1 #758e84`，上下 `margin 27px` | `ColorRect(42,1)` |
| 正文 | 衬线 `clamp(15px,1.5vw,23px)` / `lh 1.9` / `ls .17em` | `line_spacing = 1.9×字号 − 字形盒高`，块高按 CSS 的 `n×行高` 计 |
| 片区行 | `margin-top 28px` · `10px` · `ls .19em` · 分隔线 `16×1 #95a69a` / `gap 16px` | 同左 |
| 底部块 | `left 11% · right 8% · bottom 9.5%`，进度块宽 `min(660px,61%)` | 同左（按 bottom 反推排版） |
| 百分比 | `DIN 39px` / `ls -.035em` / `lh .9`；单位 `10px` `gap 5px`；`align-items:flex-end` + `baseline` | 数字右对齐且**右缘固定**（盒宽按 `"100"` 量），单位按基线定位 |
| 进度条 | 高 `2px`，底 `#667f7133`，填充 `90deg #396b65→#698a73(70%)→#a78d5b`，`box-shadow 0 0 9px #a6d7bd52` | `GradientTexture1D` + 两层低透明度同渐变铺底近似 2D 发光 |
| 进入动画 | `city-loader-enter 1.2s ease both`（`opacity 0→1` + `translateY(12→0)`） | `Tween` `TRANS_CUBIC/EASE_OUT`，并行两条 |
| 离场 | `.is-leaving{opacity:0}` + `transition .7s` | `Tween` 0.7s 淡出后隐藏 |

**17 段文案与进度语义**：原版 `CITY_LOADING_STAGES` 是 17 条（id / label / group），
`completion = (stageIndex + stageFraction) / 17`，原版注释写明这**不是**在测量下载字节数。
复刻版把 `stageIndex / stageFraction` 由 `CityWorld.progress_ratio()`（真实构建比例）换算，
于是既原样保留 17 段中文文案与「阶段 NN / 17」计数，进度条又贴着真实工作量走 ——
大件 GLB 加载时百分比照样会停，这与原版「大件卡住时进度条也停」的表现一致。

> 取舍说明：`CityWorld` 自身有 15 个构建步骤（`_plan_data` … `_plan_finish`），
> 与原版 17 段**不是**一一对应（例如原版的「跨水桥梁」在复刻版属于 `_plan_coastal`，
> 但排在 `_plan_roads` 之后）。硬把 15 步映射到 17 段会出现文案与实际工作错位、
> 且阶段计数会跳号（01→02→05→06→07→12→17）。因此这里按**真实进度比例**推进阶段，
> 保证 17 段文案按原版顺序完整走一遍、计数单调到 17/17。
> `set_loading_text()` 接收的真实相位名保留在 `diagnostics().loadPhase` 里，不上屏。
> 原版进度块也只有「阶段 / 推进依据」两行，所以复刻版**取消**了原先单列的资源计数行
> （`set_loading_detail()` 仍在，但不再上屏）。

**顺带补上原版的失败态**：`.city-loader-failure`（「载入中断 / 城市暂时没有载入 /
已保留当前进度」+「重新载入 ↗」按钮，且隐藏标题正文与片区行）。为此给 `CityWorld`
加了 `failed(reason)` 信号，`_plan_data` 里 `city.json` 读不到时停住并广播，
`MainGame` 接到后切到失败态；按钮行为对应原版 `location.reload()` → 重载当前场景。

### 14.2 出生点画面：缺两张卡片，且片区名不是「生活驿站」

对照原版截图逐块核对，HUD 大部分（品牌、右上三行按钮、速度表盘、小地图+路名、
底部快捷键条、城市故事卡）本来就已经对上了，缺口有三处：

**① 品牌副标题应该是「生活驿站 · 自由驾驶」。**
原版 `main.ts:201` 把 `life-sites.json` 的三个驿站 **push 进 `world.data.landmarks`**，
`area` 写死 `生活驿站`；HUD 的片区名取"离车最近地标"的 `area`，而出生点
（`city.json` spawn：−2664.2, −862.2）离海湾生活驿站只有 **60.7m**，是全场最近的地标。
复刻版没做这次 push，副标题于是永远显示别处的片区名。
→ `city_data.gd` 新增 `_append_life_sites()`，在 `_apply_landmark_exclusions()` 之后并入
（id 加 `life:` 前缀、`height 4.34`、`excludeRadius 0`，并带 `arrival/yaw` 供寻路用）。

**② 缺 `#career-invitation`（截图左上第二张卡）。**
原版 `city-career-experience.ts:27` 的入口卡：
`<small>雨停以后</small><strong>城市这么大，先把今天过好。</strong><span>去一个新的角落 · 认识一个人 · 攒自己的房间 <kbd>J</kbd></span>`，
CSS 为 `top:165px; left:var(--hud-edge); max-width:330px; padding:18px 22px;
background:#173437dc; border:1px solid #adc8b540`（**没有**左侧强调线，也没有圆角 —— 与故事卡不同）。
显隐条件照搬原版第 79 行：合约进行中 / 已有完成合约 / 顺路单进行中 / 菜单打开 /
无人机观景 / **已开出 300m** 任一条满足即收起；整卡可点，点击 = 打开城市手账。
→ `game_hud.gd` 新增 `_build_invitation()` / `_layout_invitation()` / `_refresh_invitation()`。

**③ 故事卡在剧情未开始时是 `story-entry` 形态，第三段是「梗概」。**
原版同一个位置有两种形态：未开始 → `.story-entry`（第三段 `synopsis`，内边距 `11px 14px`，
整块是按钮）；进行中 → `.story-hud`（第三段当前目标，内边距 `14px 15px`）。
复刻版只有第二种，且第三段直接显示第一步的目标文案。
→ `story_system.gd` 补 `SYNOPSIS`（把原版 `city-story-content.ts` 的梗概逐字搬过来）并在
`status()` 里暴露 `synopsis` / `active`；`game_hud.gd` 的 `_refresh_story()` 按
未开始 / 进行中 / 已完成三态切换文案与内边距，完成后第三段显示
原版 `city-story-experience.ts:143` 的「已完成 · 奖励不会重复发放」。

> 已知不一致（**未在本次范围**）：复刻版《最后一单》的 8 步台词是早期改写的版本
> （出现了「何志」这个原版没有的角色），而梗概已按原版写回阿辉 / 阿琳 / 老陈。
> 两者要一致需要把 `city-story-content.ts` 的 steps 全量重搬（含每步 lines / objective /
> 选项分支），属于剧情内容迁移，不在"启动画面与出生点画面"范围内。

### 14.3 涉及文件

| 文件 | 改动 |
|---|---|
| `shaders/loading_vignette.gdshader` | **新增**：加载画面两层渐变暗角 |
| `scripts/ui/panels_ui.gd` | 加载画面整段重写（海报 / 暗角 / 标题块 / 进度块 / 17 阶段 / 进入与离场动画 / 失败态） |
| `scripts/ui/game_hud.gd` | 新增 `#career-invitation`；故事卡双形态；`_layout_invitation()` |
| `scripts/core/city_data.gd` | 新增 `_append_life_sites()`，把生活驿站并入地标表 |
| `scripts/life/story_system.gd` | 新增 `SYNOPSIS`，`status()` 暴露 `synopsis` / `active` |
| `scripts/world/city_world.gd` | 新增 `failed(reason)` 信号；`_plan_data` 检查 `city.json` 是否真的读到 |
| `scripts/core/main_game.gd` | 连接 `world.failed` → `panels.fail_loading()` |

### 14.4 未做编译校验（沿用本项目约定）

按 `AGENTS.md`「禁止编译校验」，本次未运行 `tools/godot_check.ps1`，改动只经静态复核。
实机首次验证时建议按顺序确认：

1. 启动 1.2s 内标题块是否从下方 12px 淡入，且海报是**浅色陶土城**而不是黑屏；
2. 加载过程中右下角计数是否 `阶段 01 / 17` → `17 / 17` 单调走完，百分比与进度条同步；
3. 加载完成后是否 0.7s 淡出让出城市画面（而不是瞬间消失）；
4. 出生点左上是否依次是「深城纪 / 生活驿站 · 自由驾驶」、`雨停以后`卡、`城市故事`卡；
5. 开出 300m 后「雨停以后」卡是否自动收起；
6. 若 `city.json` 被人为改名，是否进入「载入中断 + 重新载入」态而不是空白画面。

---

## 十三、画面按原版截图一比一对齐（P0，整体观感）

> 2026-09-24。用户给出一张原版《深城纪》**日落自由驾驶**截图，要求把复刻版画面
> 按原版一比一复刻。

**方法**（这一节的所有数字都来自实机像素测量，没有一条是"看起来差不多"）：

- `tools/_shot.gd` + `tools/shot_plan.json` —— 一次启动按方案批量抓多组参数的
  截图。改一次参数重跑一次要 40 秒，批量扫描把一轮调参压到一次启动。
  支持逐张覆盖 `sun_energy / sky_energy / sky_gain / tier / exposure` 与任意
  Environment 属性。**每张图先 `_reset_env()` 再覆盖** —— 否则覆盖是累积的
  （第 2 张关了太阳、第 3 张只改天空时太阳仍然是关的），扫描曲线会互相污染。
- `tools/compare_shots.py [原版图] [复刻图...]` —— 并排输出全图平均亮度 /
  过曝像素占比 / 暗部占比 / 平均饱和度，以及天空中央列逐段均值与 12 个区域均值。

**起点与终点**（同一帧位置，原版 1920×1124 与复刻同尺寸）：

| 指标 | 原版截图 | 修复前 | 修复后 |
|---|---|---|---|
| 全图平均亮度 | **0.275** | 0.548 | 0.326 |
| 过曝像素（>0.95） | **0.2%** | 7.0% | 1.9% |
| 路面（画面左中） | **#292021** | #fef3e8（纯白） | #30201e |
| 人行道（左下） | **#2b353b** | #b9aca7 | #2e4248 |
| 天空·画面顶 | **#976451** | #bb8e86 | #8b5352 |
| 天空·近地平 | **#a8613b** | #e39c7d | #b6674d |

### 46. `applyCinematicRoad` 整段没移植：路面是半金属镜面（P0，画面）

**症状**：日落时路面糊成一片纯白（实测 #fef3e8），原版同位置 #292021。

**根因**：`data/city/roads.glb` 自带的 `asphalt` 材质是
`roughness=0.29 / metallic=0.18` —— 一个半金属镜面。原版靠
`city-road-surface.ts:applyCinematicRoad` 把它整段改写成漫反射沥青，而**移植版
从头到尾没有这一步**，于是路面把天空与低角度太阳整个镜像回来。

**修复**：新增 `scripts/world/road_surface.gd`（`class_name RoadSurface`），
逐项照搬原版：

| 项 | 值（sunset / night） | 值（day） |
|---|---|---|
| albedoColor | (.72,.76,.79) | (1.02,1.03,1.04) |
| roughness | .83 + .12 × orm.g | .66 + .19 × orm.g |
| metallic | 0 | 0 |
| specularIntensity | .22 | .55 |

**关于粗糙度取常量**：Godot 的 `StandardMaterial3D` 只能做 `roughness × texture.g`
的乘法，表达不了原版 `a + b·g` 的仿射式。实测 `asphalt-orm.webp` 绿通道
`mean 0.731 / p5 0.616 / p95 0.796`，对应粗糙度区间只有 **0.904 ~ 0.926**
（极差 0.022），所以取中值 **0.918** 作常量，最大偏差 0.012 —— 视觉不可分辨。

法线贴图 `asphalt-normal-gl.webp` 按原版接上：`normal_scale = .13`（原版
`normal.level`），`uv1_scale = 14 / (2.35 × 0.60) = 9.93`（原版
`texture.uScale`）。

### 47. 车道线材质没按原版压暗（P1，画面）

`roads.glb` 的 `roadline` 是 `albedo=(.896,.901,.854) / rough=.54`。
原版 `city-landscape.ts:176` 把它改成 `albedo=(.59,.61,.57) / rough=.85 /
emissive=0`。移植版从未调用 `applyLandscapeSurfaces`，于是车道线在暗色沥青上
白得刺眼。已在 `road_surface.gd` 一并照搬。

### 48. 天空着色器被"自创参数"替换（P0，画面）

**症状**：天空从「背阳紫 + 朝阳橙」的双色结构退化成一整片均匀粉橙，
天顶行均值 #bb8e86，原版 #976451。

**根因**：移植版把原版 `szSky` 的两个**写死常量**改成了外部 uniform，并自己
调了一版：天顶色写成 `(0.20,0.30,0.52)`（原版 `(.047,.12,.25)`）、地平线写死
一个暖色（原版按 `west = pow(dot(p, sd), 4)` 在 `(.42,.24,.35)` 与
`(.94,.32,.115)` 之间混）、`h` 的 smoothstep 上界写成 0.85（原版 0.72）、云换成
了完全不同的公式（无 domain warp、按仰角遮罩）。

**修复**：`shaders/sky.gdshader` 改为原版 `city-world.ts:230-242` 的**逐行译本**，
语句顺序与常量一字不改；`sky_system.gd` 去掉那两个 self-made uniform。

### 49. 天空球的太阳方位常量缺 z 轴符号修正（P1，画面）

原版 `vec3 sd = normalize(vec3(-.95,.16,-.18))` 是写在 **Babylon 世界坐标**
（z = +北）下的常量。本项目世界约定是 `z = −北`（README 第三节），照抄会把日落
辉光的方位角翻到天空另一侧 —— 实测辉光中心落在画面 x≈1400（偏右），原版截图
里它在消失点正上方。

修复：`sd` 取 `normalize(vec3(-.95, .16, .18))`。校正后与 `DirectionalLight3D`
的太阳方位（`sun_dir_data` = (.934,−.187,.305) → Godot (.934,−.187,−.305)，
太阳在西南）互相吻合。

### 50. 环境光的双通道结构被拆错：半球光被放大 2.2 倍（P0，画面）

**根因**：原版是 `HemisphericLight(0.12) + scene.environmentTexture(HDR 立方图,
environmentIntensity .40)` 的**双通道**结构，半球光只是残余补光，主漫射来自
HDR 的球谐辐照度。移植版把 environmentTexture 只接到 Sky 的**反射**上
（`ambient_light_source = COLOR`、`sky_contribution = 0`），环境漫射另走
`ambient_light_color`，于是必须把半球光乘 2.2 去补那块丢失的漫射 —— 两条路径
的能量对不上，全图亮度是原版的两倍。

修复：`ambient_light_source = AMBIENT_SOURCE_SKY`、`sky_contribution = 1.0`，
HDR 全景能量回到原版 `CINEMATIC_LOOK.environment`（0.78 / 0.40 / 0.55），
`AMBIENT_ENERGY_SCALE` 与 `SUN_ENERGY_SCALE` 都回到 1.0。

### 51. 天空增益：Babylon 与 Godot 的 ACES 肩部压缩量不同（P0，画面）

天空着色器逐行照搬之后，天空**仍然**比原版亮一倍（地平线朝日处 #fab493
vs 原版 #a8613b）。这不是着色器的问题 —— 差的是两家 ACES 对 0.42~0.94 这段
线性色的肩部压缩量：Babylon 压到显示端 0.30~0.70，Godot 几乎不压。

修复：天空着色器加 `sky_gain` uniform，`SkySystem.SKY_GAIN = 0.42`。
标定依据是天空中央列逐段扫描（见下表），0.40 时三段同时落位，取 0.42 略保守：

| y 区间 | 原版 | sky_gain=0.40 |
|---|---|---|
| 0–30 | #976451 | #87504f |
| 150–190 | #9f5c39 | #a15c4c |
| 270–305 | #a8613b | #b16349 |

### 52. 跨海对岸 / 林冠的材质统一处理没移植（P2，画面）

- `opposite-shore.glb` 的 `distant-shore`：原版 `city-world.ts` 把
  `albedoColor` 写成 `(.10,.15,.14)`、`environmentIntensity = .35`，压成一条暗
  剪影。移植版保持 GLB 自带的 `(.373,.461,.424)`，黄昏时在地平线上糊成一条明显
  偏亮的灰绿带。
- 林冠（`city-canopy.ts`）：原版对所有 `canopy_*` 统一 `metallic = 0`、
  `roughness = max(.86, 原值)`、`specularIntensity = .28`。

两者都已并入 `road_surface.gd`。**注意林冠是 `MultiMeshInstance3D`**，
材质挂在 `multimesh.mesh` 的各个 surface 上 —— 只扫 `MeshInstance3D` 的
`GlbLoader.meshes_of()` 找不到它们（实测 canopy 命中 0 份）。

### 53. HUD 整体版式与原版不同（P1，画面）

**症状**：原版是「左上宋体标题 + 右上三行快捷键 + 左侧城市故事卡片 +
左下圆形雷达 + 右下圆形表盘 + 底部居中快捷键条」；移植版是「左下弧形表盘 +
左上方形小地图 + 顶部居中模式文字」。

**修复**：`scripts/ui/game_hud.gd` 按原版 `city-hud.css` +
`city-quicktips.css` + `city-story.css` 的**实测数值**重写：

| 项 | 原版 CSS | 1920×1124 实得 |
|---|---|---|
| `--hud-edge` | clamp(22px, 1.7vw, 40px) | 32.64px |
| `--hud-dial` | clamp(224px, 16vw, 308px) | 307.2px（高 ×.9643） |
| `--hud-map` | dial × .865 | 265.7px |
| header 内边距 | clamp(25px, 2.5vh, 40px) | 28.1px |
| 城市地图 / 城市生活 / 画质 | top 33 / 69 / 94px | 同 |
| 小地图 | bottom 49px，left = edge − 10px | 同 |
| 表盘 | right = edge，bottom 20px | 同 |
| 城市故事卡片 | top clamp(170, 28vh, 340)px，宽 236px | 314.7px / 236px |
| 快捷键条 | left 50%，bottom 19px，translateX(−50%) | 同 |

表盘几何逐项照搬原版 `createDial()`（SVG viewBox `0 0 280 270`）：圆心
(140,136)、外弧 r=120、起始 −128°、扫过 256°、内圈 r=111、41 根刻度
（每 8 根主刻度，r 112→108／102，主刻度标签 r=94 值 i×5）、限速弧
118°→128°（#d87477 宽 5）、能量圈 (140,231) r=16 与闪电路径。

细节上还有两条只能绕开的差异：
- CSS 的 `letter-spacing` 在 Godot 的 Label 上没有对应属性，改用
  `FontVariation.spacing_glyph`（= em 值 × 字号）。
- 城市故事卡片的 `border-left: 2px solid #c7d4a7` 靠 `StyleBoxFlat` 表达不了
  （它只有一个 `border_color`），因此卡片边框统一 1px，左侧那条 2px 强调色
  单独用一个 `ColorRect` 叠在卡片左边缘，高度在 `update_hud` 里跟着卡片同步。

### 54. 小地图是左上方形，原版是左下圆形雷达（P1，画面）

原版 `#minimap` 是 `border-radius: 50%` 的圆形，内容按**逐扫描线透视压扁**投影：

```js
c = cos(tilt), s = sin(tilt)                       // tilt = .40 + min(1,|v|/28)×.25
project(y) = { q = (y-208)/(c + (y-208)*s/480), stretch = 1 - q*s/480 }
对 y 每 2px 一行 drawImage(plane, 512-160*stretch, 740+project(y).y, …)
```

Godot 的 `Control.clip_contents` 只能裁矩形，做不出圆角。实现分三步：

1. 地图内容画进一个 1024×1024 的 `SubViewport`（= 原版 source plane），
   用 `draw_set_transform(PLANE_ORIGIN×.75, −yaw, .75)` 复现原版
   `translate(512,740); rotate(−yaw)` 的变换；
2. 显示端是一个 `TextureRect`（= 原版 320 输出），挂
   `shaders/minimap.gdshader`：该映射**逐行可逆**，所以在片元里直接求逆
   （`q = dy/(c+dy·s/480)`）采样 plane，再叠圆形裁切（圆心 (160,160) r=159）
   与顶部雾霭渐变（y 0→245，rgba(48,83,99,.30) 渐隐），底色 #19343e；
3. 车辆箭头、`N` 标记、路名标签画在显示端**之上** —— 与原版一致，
   它们不受透视压扁影响。

`N` 标记位置照搬原版：`left = (50 − sin(yaw)×45)%`、`top = (50 − cos(yaw)×45)%`。
`evenodd` 挖洞用"外环填色 + 内环按 plane 底色再填一遍"等价实现（Godot 的
`draw_colored_polygon` 不支持带洞多边形）。

### 55. 小地图路名标签一直是空的（P2，画面）

`road.get("display_name", road.get("name",""))` —— **`get` 的默认值只在键不存在
时生效**。本工程的道路字典里 `display_name` 存在但可能是空串（同名道路靠
`loadRoadDisplayNames` 补），于是直接返回空串，标签永久为空。
改为「先取 display_name，为空再退回 name」，与 `game_hud._road_name` 的写法一致。

### 56. HUD 比 MapUI 先建：圆形小地图停在 0×0（P1，画面）

`MapUI.setup()` 晚于 `GameHUD.setup()`，而 HUD 的 `_build()` 里那次排版就去找
MapUI 要小地图位置（`set_minimap_rect`）—— 那时 `minimap_holder` 还是 `null`，
请求被丢掉，之后没有任何机制会重发，小地图就永远不显示。

修复：`GameHUD.refresh_layout()` 公开入口，`main_game._on_built()` 在
`maps.setup()` 之后再调一次。

### 验证

- 全流程无脚本错误 / 无解析错误，`python tools/validate_project.py` 通过。
- 同尺寸（1920×1124）截帧量化：全图平均亮度 0.548 → **0.326**（原版 0.275）；
  过曝像素 7.0% → **1.9%**（原版 0.2%）；天空中央列 8 段逐段对上（见上文表）。
- HUD 六个区块（标题 / 右上三行 / 故事卡片 / 圆形雷达 / 圆形表盘 / 快捷键条）
  全部出现且位置与 CSS 推算值一致；`dump_ui()` 实测
  `minimap_holder pos=(22.64, 809.27) size=(265.73, 265.73)`、
  路名标签 `text='滨海大道 | Binhai Blvd'`。

### 本轮仍未对齐的部分（如实列出）

| 项 | 原版 | 本版 | 说明 |
|---|---|---|---|
| 天空云的形态 | 细碎横向云丝 | 更成块、更亮 | 云的公式已逐行照搬，差异来自 ACES 肩部（同 #51） |
| 建筑立面 | 31 个材质 profile + 烘焙 AO | GLB 自带材质 | `city-architecture-materials.ts`、`city-ambient-occlusion.ts` 仍未接入，建筑实测比原版亮约 1.7 倍 |
| 远景雾霭 | 逐段调过 | 略亮 | 实测"远山/地平"区域比原版亮约 2 倍 |
| 林冠亮度 | 暗剪影 | 偏亮偏绿 | 材质参数已按原版统一，剩余差异来自 IBL（Godot 的 `StandardMaterial3D` 没有逐材质 `environmentIntensity`，原版林冠是 .82、沥青是 .55） |
| HUD 绝对尺寸 | 小地图直径 246px | 265.7px | 原版截图来自**另一个构建**（其副标题是「生活驿站 · 自由驾驶」，而当前 `city-hud.ts` 写的是「深圳湾 · 自由驾驶」），其 CSS 常量与当前源码不同。本版按**当前源码的 CSS** 取值 |

---

## 十二、汽车过桥时穿模到桥下（P0）

> 2026-09-23。用户反馈：汽车在路上移动，过桥的时候会穿模到桥下。

桥梁标高这一层是移植 `src/city-coastal-infrastructure.ts` 的 `createBridgeHeightSampler`。
原版只有 15 行，移植版漏了其中**三处**关键语义，叠在一起就表现为"车从桥面掉到桥下"。

### 43. 桥梁抬升传错了距离：用了"到折线首点的距离"而不是"到桥段的垂直距离"（P0）

**文件**：`scripts/world/height_field.gd`（`bridge_lift()`）

原版：

```js
bridgeRamp(closest(x, z, s.a, s.b).d, s.height, s.ramp)   // d = 点到【线段】的最短距离
```

旧移植版：

```gdscript
var along := p.distance_to(a)          # ← 到【折线首点 a】的距离
if lateral > c["width"] * 0.5 + 6.0:   # ← 9m 窄门，不是原版的 onRoad 判据
    continue
var u := along / ramp
lift = height * (1 - u * u * (3 - 2 * u))
```

`bridgeRamp(d)` 的本意是：桥面在 `d≈0`（正上方）取满高 2.8m，再沿**横向** 70m
三次平滑衰减到 0 —— 这条衰减带**同时就是引桥**（车沿路驶近桥头时，到桥段的距离
从 70m 连续降到 0，路面随之从地面平滑抬到桥面，桥头不会出现台阶）。

旧写法把 `d` 换成"到首点 a 的距离"，于是"桥面"变成以 a 为圆心、半径 70m 的圆盘，
再被 9m 窄门削成一条细条：**桥中段和整条引桥的标高都是 0**，车一上桥就掉回地面。

同时 `_crossings` **只取了 `pts[0]`/`pts[1]`**，而 crossings 是折线
（164 条里不少是 3~9 点：笋岗东立交 9 点 74m、人民路 5 点 61m），折线后半截完全没标高。

**修复**：按原版逐段登记（258 段），并把 `d` 改回"到线段的最短距离"。

### 44. `heightAt` 把桥梁标高**加**了上去，原版是取 **max**（P1）

**文件**：`scripts/world/height_field.gd`（`height_at()`）

原版 `heightAt: (x,z) => Math.max(base(x,z), bridgeHeight(x,z))` —— 桥面是**固定标高**
（`height` = 2.8m），不是"在起伏地形上再叠 2.8m"。旧实现写的 `base += bridge_lift(...)`，
桥下水深为负 / 地形抬高时车身高度会跟着漂，也和 `roads.glb` 里烘好的桥面几何对不上。

已核对原版组合顺序（`city-world.ts:250~261`）：detail → mountains → groundRelief →
coastal(**max**) → 咖啡馆地板(**最后叠加**)；移植版现在的顺序与之逐层一致。

### 45. 缺 onRoad 闸门 + 缺 `grade >= 0` 过滤（P1）

**文件**：`scripts/world/height_field.gd`（`_build_gate_index()`）

原版桥面标高**只在路面上生效**：

```js
for(const r of data.roads) if(Number(r.grade) >= 0) ... add(roads, ..., r.width/2 + 8);
const onRoad = roads.get(k)?.some(s => closest(x,z,s.a,s.b).d <= s.width/2 + 8);
return onRoad ? height : 0;
```

两道门都缺：不加 onRoad 门，桥两侧 70m 范围内的水面/绿地/人行道会一起鼓起来；
不加 `grade >= 0` 过滤，本城 12202 条道路里那 **177 条负 grade**（-1/-2/-3/-4，
地下/下穿路段）会被当成"路面"，把桥面抬升漏给桥底下穿的路。

索引做法照搬原版（100m 均匀网格）。为避免全量索引 1.2 万条道路的启动开销，
只收录"外扩 `width/2+8` 后能碰到某个已登记桥格"的路段 —— 判定与查询共用同一套格子，
不会漏段（实测收录 **3476 段，与参照实现完全一致**）。

### 验证：离线复刻原版 TS 算法做差分

用 Python 逐行照搬原版 `createBridgeHeightSampler`（含索引与两道门），
对 **30768 个采样点**（164 条桥的沿段法向 10 档偏移 + 桥头延长线 6 档 + 3000 个随机点）
逐点比对 `bridge_lift()`：

```
路面门段 3476 = 参照 3476 ✅（索引完全一致）
最大绝对误差 1.46e-05 m（float32 端点舍入量级，1 微米级）
误差 > 1e-4 的 240 个点全部是"横向偏移正好 = width/2+8 = 11m"的刀锋点
（采样按构造正好落在门的边界上，float32/float64 末位差异会把布尔门翻过去）。
```

### 验证：沿路桥面剖面（同一条路，旧实现 vs 原版）

笋岗东立交 `way/44426582`（5 点折线，125m）：

| 沿路距离 | 原版/修复后 | 旧实现 |
|---:|---:|---:|
| 0 m | 2.129 | **0.000** |
| 20 m | 2.792 | 2.792 |
| 30 m | 2.800 | 2.703 |
| 50 m | 2.800 | 2.687 |
| 70 m | 2.800 | **0.653** |
| 80 m | 2.800 | **0.215** |
| 90 m | 2.800 | **0.006** |
| 100 m | 2.800 | 2.773 |

新洲路 `way/40972430`（3 点折线，172m）：旧实现从 30m 起整段掉到 0，新实现
2.800 → 2.723 → 2.382 → 1.950 → 1.410 → 0.847 → 0（这就是引桥的下坡）。

### 验证：端到端（真项目里把车摆到桥上逐点读车模高度）

```
笋岗东立交：
   0.0 m  地面 2.129  车模Y 2.149
  20.1 m  地面 2.792  车模Y 2.812
  32.2 m  地面 2.800  车模Y 2.820
  50.3 m  地面 2.800  车模Y 2.820
  80.4 m  地面 2.800  车模Y 2.820
 104.6 m  地面 2.800  车模Y 2.820
 125.0 m  地面 2.800  车模Y 2.820
桥中点 (5379.8, 1599.0)：地面 2.800 / 车模Y 2.820
桥面最低点 = 2.129 m（旧实现中段是 0.000）
crossings 索引构建 42 ms / 段 258 / 路面门段 3476
单次 bridge_lift 查询约 11 µs（含 JSON 取值开销）
```

顺带确认 `coastal-bridges.glb` 的桥面几何在世界空间落在 y≈2.75~3.0，与标高场的
2.8 对得上 —— 车现在骑在桥面上，而不是钻进桥底。

全部临时探针文件已清理；全项目 `--headless` 启动 SCRIPT/PARSE/COMPILE ERROR 0 条。

---

## 十一、大地图上的文字全是方块乱码（P1）

> 2026-09-23。用户反馈：在大地图上点击选点、路线规划完成之后，显示的文字是乱码。

### 41. 当帧新建的 `SystemFont` 当帧就 `draw_string`，画出来是实心方块（P1）

**文件**：`scripts/ui/map_ui.gd`（`_font()` / `_build()`）

**现象**（实机截图放大后确认）：地图上由 `CanvasItem.draw_string` 画的**每一段**文字
—— 地标名、比例尺（`849 m`）、路线信息行（`已规划到 … · 沿道路约 4.7 公里 · 抵达目的地周边`）、
底部操作提示（`M 关闭 · 滚轮缩放 · …`）—— 全部是一格一格的**实心方块**，
而同一时间 `Label` / `Button` 上的中文（HUD、地图里那两个按钮）完全正常。

**最小对照实验**（Godot 4.7.1，一个 Control 的 `_draw` 里画 5 行同一句话）：

| 行 | 字体来源 | 结果 |
|---|---|---|
| ① | **本帧** `SystemFont.new()` + 设 `font_names`，本帧 draw_string | **实心方块** |
| ② | 构建时建好，隔帧再用 | 正常中文 |
| ③ | 上一帧建好，本帧用 | 正常中文 |
| ④ | 本帧新建的 SystemFont 外面包一层 `FontVariation` | **实心方块** |
| ⑤ | 同一个新建字体交给 `Label` 用（下一帧才绘制） | 正常中文 |

即：**`SystemFont` 的系统字体解析是延迟的**，新建那一帧内还没拿到真实字形，
TextServer 只能画出"缺字方块"；跨过一次帧边界就好了。④ 说明这跟 `draw_string`
还是 `Label` 无关，只跟"字体对象有没有跨帧"有关。

**根因**：`_font()` 原来是**每次调用都新建**，而 `_draw_big_map()` 每次重绘要调 4 次
（16 / 15 / 18 / 14 号）。每帧都新建 → 每一次都是"那一帧" → 永远等不到跨帧，永远方块。
HUD 和地图按钮之所以正常，是因为它们的字体在 `_build()` 里建好、隔了很多帧才绘制。
这也解释了"为什么只有大地图上的文字乱码"。

**修复**：`_font()` 改为按字号缓存，并在 `_build()` 里一次性预热全部字号：

```gdscript
var _font_cache := {}
const FONT_SIZES := [14, 15, 16, 18, 20]      # _draw_big_map / 路线按钮用到的全部字号

func _font(size: int) -> Font:
	var hit: Font = _font_cache.get(size)
	if hit != null:
		return hit
	var sf := SystemFont.new()
	...
	_font_cache[size] = sf
	return sf

func _build() -> void:
	for s in FONT_SIZES:      # ⚠️ 必须赶在第一次绘制之前建好
		_font(s)
```

顺带把开销也降下来了：原来每次重绘都要重新枚举一遍系统字体。

**验证**：实机截图逐区域放大 —— 信息行 `已规划到 七街公馆 / 哈尔滨大厦`、
`· 抵达目的地周边`、比例尺 `849 m`、底部提示行、目的地标记旁的名字，全部为正常中文。

### 42. 地图层级低于 HUD，两段文字叠在一起（P2，同一处）

**文件**：`scripts/ui/map_ui.gd`（`setup()`）

原版 `src/city-map.css:4` 给地图面板 `z-index: 12`，而 `src/city-hud.css` 的 HUD 只有
`z-index: 2~6` —— **地图一开就压住 HUD**。移植版给 `MapUI` 的是 `layer = 8`（HUD 是 10），
方向正好反了：HUD 的速度表、里程条压在左下角比例尺上，底部 toast 压住路线信息行，
两段文字叠在一起，看起来同样是"乱码"。

**修复**：`layer` 8 → **11**（高于 HUD 的 10，低于 PanelsUI 的 12，手账/设置页/加载画面
仍在其上）。

**验证**：
```
地图展开时：
  滚轮      → 地图缩放 0.11785 → 0.13553 ✅   相机 distance_scale 1.0 → 1.0 ✅
  左键拖动  → 地图平移 ✅
关闭地图后：
  滚轮      → 相机 distance_scale 1.0 → 1.08 ✅（游戏输入未被一起关掉）
截图：比例尺 / 提示行 / 信息行 / 按钮文字全部清晰，HUD 不再压字。

全项目 --headless：SCRIPT ERROR / Parse Error / Compile Error 0 条。
```

---

## 十、大地图上的鼠标事件穿透到游戏画面（P1）

> 2026-09-23。用户反馈：大地图打开时，鼠标在大地图上的所有操作都会穿透到游戏画面里。

### 40. `MOUSE_FILTER_STOP` 拦不住滚轮，事件照样走到 `_unhandled_input`（P1）

**文件**：`scripts/ui/map_ui.gd`（`_on_map_input`）、`scripts/core/main_game.gd`（`_unhandled_input`）

大地图 `big_map` 是铺满全屏的 `Control`，`mouse_filter = MOUSE_FILTER_STOP`，
按直觉它就该吞掉所有鼠标事件。但**实测不是**。

**最小工程复现**（Godot 4.7.1，一个 CanvasLayer + 一个铺满的 STOP Control，
用 `push_input` 灌入合成事件，同时打印 `gui_input` 与 `_unhandled_input`）：

| 事件 | `gui_input`（地图侧） | `_unhandled_input`（游戏侧） |
|---|---|---|
| 地图隐藏 | — | 左/右键、滚轮、移动、抬起（全部） |
| 地图显示 | 左/右键、**滚轮**、移动、抬起 | 左/右键 ❌ 被吞、移动 ❌ 被吞、**滚轮 ✅ 漏过去** |

即：`MOUSE_FILTER_STOP` 会自动吞掉**按键**与**移动**事件，**唯独滚轮不管**。
后果就是在地图上滚滚轮时，地图按 1.15 倍缩放了，`main_game._unhandled_input`
里那份滚轮也照跑一遍 —— 相机 `distance_scale`（或步行模式的 `walk.zoom`）跟着一起动。

**修复**（两道闸）：

1. `map_ui._on_map_input()` 进门第一件事就是把事件标记为已处理：

```gdscript
func _on_map_input(event: InputEvent) -> void:
	if event is InputEventMouse:
		big_map.accept_event()
	...
```

（`accept_event()` → `Viewport.set_input_as_handled()`；同一次实测里加上它之后
`_unhandled_input` 收到 0 条鼠标事件。按键/移动本来就会被 STOP 吞掉，但统一处理
可以免受引擎版本行为变化影响。）

2. `main_game._unhandled_input()` 加兜底闸门，地图展开期间整体不碰相机：

```gdscript
	if maps != null and maps.big_map_visible and event is InputEventMouse:
		_dragging = false
		return
```

这一道是为"事件从别的控件漏下来"准备的 —— 地图里的「自动驾驶前往 / 自己开过去」
两个 `Button`、以及将来新增的面板，都在 `big_map` 的更上层，滚轮落在那儿时
`big_map.gui_input` 根本不会被调用，`accept_event()` 也就无从谈起。

3. 顺带修掉一个连带的**粘滞拖拽**：正按着右键转视角时按 M 开图，之后右键松开
落在图上、被地图吞掉，`main_game` 永远收不到那个"抬起"，`_dragging` 卡在 `true` ——
关图后不按任何键移动鼠标，视角也会跟着转。`map_ui` 新增 `big_map_toggled` 信号，
开图瞬间由 `main_game._on_big_map_toggled()` 把 `_dragging` 归零。

**验证**（端到端，真项目 `res://scenes/main.tscn` 实例化后灌合成事件）：

```
地图展开时：
  滚轮     → 地图缩放 0.11785 → 0.13553 ✅  相机 distance_scale 1.0 → 1.0 ✅
  左键拖动 → 地图平移 big_view (-3254.5,-419.5) → (-3697.2,-124.4) ✅  相机未动 ✅
  右键按下 → main_game._dragging 保持 false（未启动相机拖拽）✅
关闭地图后：
  滚轮     → 相机 distance_scale 1.0 → 1.08 ✅（游戏输入未被一起关掉）
拖拽中途按 M 开图 → _dragging true → false ✅

全项目 --headless 启动：SCRIPT ERROR / Parse Error / Compile Error 0 条。
```

---

## 九、"从路面顶到天空"的纯色幕墙（P0，画面）

> 2026-09-23。用户反馈：沿滨海大道开两三公里后，远处出现一片**从路面一直顶到天空的纯色**
> （黄昏橙），把远处城市切成一刀，观感像一堵竖直幕墙挡住远景。

### 39. 天空球从未跟随相机，球心永远钉在世界原点（P0）

**文件**：`scripts/core/main_game.gd`（`_process`）

`sky_system.gd` 的程序化天空球直径 8000（半径 4000），靠 `follow(cam_pos)` 每帧把球心
搬到相机上来模拟原版 Babylon `atmosphere` 的 `infiniteDistance`（永远在无限远）。
但这条路径**全项目没有任何调用点**：

- `city_world._update_subsystems()` 的驱动列表是
  `[traffic, pedestrians, ebikes, signals, weather, water, scenery, distant, signs]` —— **不含 `sky`**；
- `main_game._process()` 只调了 `world.lighting.update_system()` 与 `world.facades.update_system()`，
  **漏了 `world.sky`**（`sky.update_system()` → `follow()` 是唯一的实现入口）。

于是球心永远停在 `(0,0,0)`，而出生点世界坐标是 `(-2664.2, 1.5, 862.2)` ——
距原点 **2800 m**，即球半径的 70%，玩家一直在球内大幅偏心处。

**离线复算**（球心 O=(0,0,0)、R=4000、相机 P=(-2664,1.5,862)、朝西）：
视线与球面的交点几乎全部落在球的赤道附近（0° 仰角时命中距离只有约 **1200 m**），
着色器里的 `p.y = 局部y / R` 全程极小：

| 视线仰角 | 命中点 p.y | `h = smoothstep(-0.05, 0.85, p.y)` | 结果 |
|---:|---:|---:|---|
| 0°（地平线） | 0.0004 | 0.009 | **100% horizon_color** |
| 20° | 0.108 | 0.082 | **92% horizon_color** |
| 40° | 0.232 | 0.232 | 77% horizon_color |

即从路面到画面顶部都是同一个 `horizon_color`（黄昏 = `(0.86,0.55,0.36)`），
天空渐变压根看不出来 —— 这正是"纯色幕布"的来源。

> 注意：远处建筑**并没有被几何遮挡**。天空球是 `depth_draw_never`（不写深度），
> 远景分块该画还是画；被吃掉的是远景的**层次与空气感**，所以主观上像被挡住了。

**修复**：`main_game._process()` 在 `lighting` 之后补一行（用相机位置，与 lighting 同类）：

```gdscript
	if world.sky != null:
		world.sky.update_system(dt, focus)
```

**验证**：
```
Godot 4.7.1 --headless --editor --quit（scripts/shaders/scenes 精简副本）
  → update_scripts_classes: 44 steps 全部注册，exit=0
  → SCRIPT ERROR / Parse Error / Compile Error 0 条
```

---

## 八、地图路线规划与自动驾驶不可用（P1）

> 2026-09-23。用户反馈：大地图路线规划、自动驾驶现在用不了。
> 第 28 条那次修复只做了 `Autopilot` 的**闭环数学验证**（车模型 + 路网模拟开 8.6km），
> UI 侧的坐标与剔除**一行都没验过** —— 问题全漏在这一层。

### 29. `screen_to_map` 的 y 分量括号位置错了（P0）

**文件**：`scripts/ui/map_ui.gd`

`map_to_screen` 是 `sy = H/2 − (py − vy)·k`，逆变换应为 `py = vy − (sy − H/2)/k`，
`view.y` **不参与除法**。旧写法 `(view.y - (s.y - size.y*0.5)) / map_scale` 把
`view.y` 也套进了除法。

离线复算（真实 extent，1280×720）：

| 视图中心 | zoom | 逆变换最大误差 |
|---|---|---|
| (0, 0) | 任意 | 0（碰巧正确） |
| (1200, −800) | 0.6 | 533 m |
| (1200, −800) | 0.055 | 13 745 m |
| (−4000, 2000) | 0.055 | 34 363 m |

后果：滚轮缩放的锚点整屏飞走；用它算出的视野矩形整个错位。

### 30. 视野剔除矩形用错了角点（P0）

`Rect2` 的 position 是**左上角**（x 最小），而右下角 `br` 的 x 是最大的。
旧写法 `Rect2(br, (tl - br).abs())` 让矩形向右平移了整整一个屏宽 ——
**实测屏幕内可见点被剔除的比例为 100.00%**，即大地图上道路/建筑一条都不画，
打开只剩水域底色和陆地轮廓。改为 `Rect2(tl, Vector2.ZERO).expand(br)` 后为 0.00%。

### 31. 默认缩放与地标可见性（P1）

- 打开大地图固定 `big_scale = maxf(base_scale(), 0.6)`：0.6 只显示约 2.1km 宽，
  而城市宽 13.5km（占 15.7%），看不到路网全貌。改为铺满全城的 `base_scale()`
  （并按**实际窗口宽度**算，不再写死 900）。
- 地标标注门限 `zoom ≥ 1.1`，默认 0.6 下**一个地标都看不到**，也就无从点选。
  改为圆点始终绘制、名称仍在 zoom ≥ 1.1 时才画。
- 比例尺标签恒为 "100 m"（`scale_len` 又被乘回 `big_scale`），改为真实米数/km。

### 32. 点选目的地只能命中地标，点别处毫无反应（P1）

命中半径 26px 内没有地标就直接 `return`，玩家点了地图却没有任何反馈。
改为：**地标优先**（仍用 `arrival`）→ 否则把点击处吸附到最近道路
（`world.collision.nearest`，超 90m 视为海面/山体）→ 仍失败则发
`pick_failed` 信号，由 `main_game` toast 明确提示。

### 33. 路网有 1102 个连通分量，孤岛端点必然规划失败（P1）

**文件**：`scripts/world/road_graph.gd`

离线复算 navigation.json：42829 节点 / 47218 边，最大连通分量 35558 节点
**仅占 83%**，其余 7271 个节点分属上千个小区/园区孤岛。
抽样「主分量起点 → 孤岛终点」25 组：**兜底前可达 0/25**。

**修复**：建图时算一次连通分量（`_build_components`）＋ 128m 节点网格
（`_build_grid`）；`route()` 里把落在孤岛上的一端整体吸附到主分量最近节点
（t/length 归零，等价于"从该节点出发/到达"）→ **兜底后 25/25 可达**。

### 34. 寻路性能（P2）

- `_MinHeap` 由 `Array[Array]` 改为两条平行的 **Packed** 数组
  （`_id` / `_cost`），避免每次比较两层 Variant 解引用；
- Dijkstra 增加**提前退出**：终点两端都出堆即收工（一致代价搜索下出堆即最终
  最短路）。离线比对：终点最短路与完整 Dijkstra **0 处不一致**；
- `nearest_edge` 增加网格加速（带回退全扫）。实测（路网附近的点，360 个采样）
  **0 处偏差、62× 加速**；远离路网时 3 圈内自动退回线性全扫，结果不变。

### 35. `_MinHeap._ensure` 的扩容判断写反了（P0，**上一轮自己引入的回归**）

**文件**：`scripts/world/road_graph.gd`

上一轮第 34 条把堆从 `Array[Array]` 改成 Packed 数组时，`_ensure()` 写成了
`if _id.size() >= cap`（容量**够**才扩）。正确应为 `<`（容量**不够**才扩）：

```
第一次 push：_id.size()=0 >= 1 为假 → 不扩容 → _id[0] = id 越界
  → Index p_index=0 out of size (0) → Dijkstra 整个挂掉 → route() 返回空
```

**这正是用户第二轮反馈「点击后提示无法规划到该目的地的路线」的直接原因。**

教训：上一轮用 Python 复算验证时，参考实现里这一行**写的是正确的**，
所以离线验证 0 失败，而 GDScript 侧崩了 —— 复算脚本必须逐行照抄待验代码，
任何"顺手写对"的地方都会让验证失效。已补一条针对性验证：

| 版本 | 结果 |
|---|---|
| 错误版（`>=`） | 第一次 push 即 `Index p_index=0 out of size (0)` |
| 修复版（`<`） | 5000 次 push/pop 全部取出、cost 单调不减、逐项与排序结果一致 |

顺带补两处健壮性：
- `city_world._plan_data()`：navigation.json 建图失败时退回 `build_from_roads()`
  （原版本来就有这两条路径），不再让 graph 静默为空；
- `autopilot.start()`：失败时 `push_warning` 打出起点/终点/路线点数与
  `no-graph` / `no-route` 原因，下次不用再猜。

### 36. 地图点选吸附用错了索引（P1，上一轮引入）

点空白处时用了 `world.collision.nearest` —— 那是**碰撞判定**用的，只在点所在的
**单个 90m 格子**里找、不向外扩散，点稍微偏一点就返回空。
原版对应的是 `MapRoadIndex.nearest`（道路线段索引，向外扩散 8 圈 + 全扫兜底）。
改为 `world.graph.nearest_edge()`。

### 38. 规划完成后可选「自动驾驶前往 / 自己开过去」（对齐原版两个按钮）

> 2026-09-23。用户要求：路径规划完之后要能选驾驶方式。

**原版做法**（`city-map.ts` / `main.ts`）：

```html
<button id="auto-drive">自动驾驶前往 ↗</button>
<button id="drive-route">自己开过去 →</button>
```

```ts
onSelect: selectDestination            // 选点瞬间就算出 route，小地图画浅绿预览线
onAutoDrive:   → world.startAutoDrive(destination)
onManualRoute: → world.cancelAutoDrive('manual-route'); toast('沿小地图上的浅绿路线行驶')
manual.disabled = auto.disabled = !hasRoute   // 没有路线时两个按钮都禁用
```

即 **选点 → 立即出预览路线 → 再选驾驶方式**，而不是点一下就直接开走。

**Godot 版原实现**：点选即 `start_autopilot()`，没有"自己开"这个选项。

**改造**（用**按钮**而不是键盘 —— 首版做成了按 1/2/3，用户明确要求照原版点按钮）：

- `autopilot.plan(from, target)`：只规划、不启动，返回预览路线；
  `autopilot.start(target, from, precomputed)` 可沿用预览路线 ——
  既省掉第二次全图 Dijkstra，也保证预览线与实际行驶的是同一条。
- `map_ui.gd`：大地图底部中央新增两个 `Button`「自动驾驶前往 ↗」「自己开过去 →」
  （原版 .atlas-route-actions）。它们是 `big_map` 的子控件，点完由
  `main_game` 关地图 —— 与原版 `onAutoDrive/onManualRoute` 里
  `selectDestination(...) 再 openMap(false)` 的顺序一致。
  按钮上方画一行路线概览（原版 `#destination-status`）：
  "已规划到 X · 沿道路约 3.2 公里 · 抵达目的地周边" / "目的地就在附近"。
  规划失败则 `clear_destination()`，按钮不出现。
- **选中后地图不关闭**，等玩家点按钮；关地图只是收起按钮，目的地仍有效
  （对应原版 `selected` 保留），按 M 重开地图时按钮会带回来。
- 「自动驾驶前往」：车若已挪开 >50m（先自己开了一段）则重算路线；
  「自己开过去」：`autopilot.cancel()` 但**保留路线**让玩家沿它开，22m 内判到达。
- 下车 / 进观景 / 换坦克 / R 回正路面 → 清除待选状态。
- 小地图路线颜色由金色改为**浅绿** —— 原版提示语就是"沿小地图上的浅绿路线行驶"。
- HUD 常驻行改为「待出发 · X · 按 M 打开地图选择驾驶方式」（地图关掉时不至于无路可走）。

### 37. 自动驾驶重规划没有冷却（P2）

`_wait_timer > 3s` 就重规划，堵车时会**每 3 秒跑一次全图 Dijkstra**，
表现为自动驾驶开着开着周期性卡一下。接上此前声明却从未使用的
`_reroute_cooldown`，冷却 8 秒。

---

## 七、性能优化（P1）：卡顿与掉帧根因逐项修复

> 2026-09-22。目标：消除运行期周期性尖峰（卡顿）与持续掉帧。
> 方法：静态热点勘察 → 逐项修复 → `--headless --import` 编译校验、
> 1800 帧空跑、以及「焦点每 40 帧移动 150m + 开雨 + 切画质档」的
> 临时驱动场景（跑完已删除），全程零脚本错误、exit=0。

### 15. 电摩停车排每 0.5s 无条件全量重建（周期性尖峰主嫌疑）

**文件**：`scripts/world/ebike_system.gd`

`update_system` 节流到 0.5s 后**没有跳过条件**：静止时也每 0.5s 执行
「网格查询 → 距离排序 → 700 次 `ground_height()`（五层串行采样）+ 700 次
`set_instance_transform`/`set_instance_color`」。而车是**静止的**，高度和颜色
永远不变。

**修复**：
- 加 `REBUILD_MOVE = 30m` 阈值：焦点没挪 30m 且视角模式没变 → 整轮跳过；
- 每车 `Transform3D` / 颜色按车下标缓存（`_bike_xf` / `_bike_col`），只算一次；
- 排序查询换 `PointGrid.query_radius_sorted2`（Vector2 承载，零字典分配）。

### 16. 信号灯每 0.5s 全量扫 395 个路口 + 逐臂重算地面高度

**文件**：`scripts/world/traffic_signals.gd`

`_rescan` 是各系统里唯一没建空间索引的：全部路口测距 + `sort_custom`
（约 3400 次 lambda 比较）+ 140 条臂各调一次 `ground_height()`。

**修复**：
- 路口建 `PointGrid`（320m 单元），半径 800→1600→4000m 逐级扩，凑满即可；
- 灯臂 `Transform3D` 缓存进 `arm["_xf"]`，只算一次；
- 重扫加 `RESCAN_MOVE = 40m` 阈值 + 3s 兜底；灯色刷新（跟随相位）不受影响。

### 17. 林冠重建每棵树都重采地面高度（高速行驶时持续掉帧主嫌疑）

**文件**：`scripts/world/scenery_system.gd`

重建预算 9000–16000 棵，分帧已做（2600/帧），但 phase 1 对每棵候选树
都调 `ground_height()` + 重建 `Basis` —— 高速时每 2.3s 重启一次，
等于持续处于「每帧 2600 次五层采样」状态。

**修复**：
- 每棵树最终 `Transform3D` 按下标缓存（`_canopy_xf`），重建只剩查表 + 追加；
- 分帧量自适应：帧率 < 50 时 chunk 减半，宁多铺几帧不撑爆单帧；
- 排序查询换零分配版本。

### 18. 碰撞热路径逐边判定每次新建 Dictionary

**文件**：`scripts/world/city_collision.gd`

`blocked()` 对建筑环**每条边**调 `closest_point()`，每次返回新 Dictionary
（含 Vector2）。建筑密集区每帧几十上百个临时字典 → GC 压力 → 偶发卡顿。

**修复**：新增零分配的 `seg_distance()`（只返回 float）专供逐边判定；
`closest_point()` 保留给 `nearest()`（每帧调用量极小）。

### 19. 雨天 = 240 个独立 MeshInstance3D + 240 份独立材质

**文件**：`scripts/world/weather_system.gd`

240 个透明 draw call + 每帧 240 次节点 transform 更新，下雨帧率直接腰斩。

**修复**：改单 `MultiMeshInstance3D` + 单 `MultiMesh` + 单共享材质
（1 个 draw call）；下落动画仍按原版公式在 CPU 更新 240 个实例变换
（每帧 <0.2ms）。`set_raining` / `update_system` / `diagnostics` 同步适配。

### 20. 画质档位的阴影参数从未真正接线

**文件**：`scripts/world/lighting_director.gd`

`set_quality` 里 `sun.shadow_enabled = true` 无条件置真——低档也跑
4-split 全尺寸阴影图。`GraphicsQuality.PROFILES` 里的 `shadow_size`
（1024/2048/2048）全项目没有任何地方消费；引擎默认阴影图集是 4096。
另外 `GraphicsQuality` 里定义的 `SHADOW_ORTHO_NORMAL/AERIAL` 也没接线。

**修复**：
- 阴影图集按档位 `RenderingServer.directional_shadow_atlas_set_size()`
  （低 1024 / 中高 2048，默认 4096 直接砍半以上）；
- 低档降 2-split、关 blend_splits；街面/高空自动切阴影绘制距离
  260m / 1050m（切档与 `update_system` 里都套用）。

### 21. 车流速度只有标称的 1/6（行为 bug，顺带修正）

**文件**：`scripts/world/traffic_system.gd`

`_sync_instances` 每 0.1s 才执行，推进却用单帧 delta（≈16ms）——
车流实际速度 7–13 m/s 变成 ~1.5 m/s。

**修复**：累积 delta、同步时一次性推进（`_sync_acc`，钳制 0.5s）。
顺带把「每车线性扫 69 个 MultiMesh 找 group」换成 group→[MultiMesh] 表。

### 22. 招牌 64 个 Label3D 每 0.25s 无条件重写文字

**文件**：`scripts/world/sign_system.gd`

赋**相同的 text** 也会触发字形重排与纹理上传，4Hz × 64 个是持续隐性开销。

**修复**：文字相同短路（`_pool_texts`）；相机 25m 内没动且昼夜模式没变
→ 整轮选择跳过。

### 23. 立面瓦片加载节奏与可见性扫描

**文件**：`scripts/world/facade_streamer.gd`

- 卸载 + 显示/隐藏两趟 O(150) 循环从每帧降到 0.25s 一次（瓦片 640m 粒度无感）；
- 两次入队之间加 2s 冷却（`LOAD_COOLDOWN`）：每块 5–7MB 的 GLB
  `instantiate()` 在主线程执行，冷却把相邻尖峰拆开，不再叠成长帧。

### 24. 排序查询的字典分配风暴（基础设施）

**文件**：`scripts/utils/point_grid.gd`

新增 `query_radius_sorted2()`：返回 `Array[Vector2]`（x=下标，y=距离），
内建类型不进堆。林冠 / 电摩 / 灯光 / 信号灯的周期重建不再每次分配
上千个临时 Dictionary。

### 性能优化验证结果

```
godot --headless --import                     → exit=0，43 个 GDScript 全部注册
godot --headless --quit-after 600/1800        → exit=0，无脚本/运行时错误
焦点驱动实测（22 次移动 × 150m + 开雨 + 切档 + 高空切换）：
  signals visibleArms=384（网格索引生效）
  traffic cars=34（车流正常推进，速度修正生效）
  scenery LOD 各桶计数正常（分帧重建在移动中正常工作）
  facades loadedTotal=13 / unloadedTotal=13（加载-卸载循环 + 冷却正常）
  weather streaks=240（MultiMesh 雨每帧更新正常）
  lighting lampLit=true（画质套用后灯光池正常）
```

### 25. 曝光过度修复（P1，光照渲染）

> 2026-09-22。用户反馈画面曝光过度。用「实机截帧 + 亮度统计」定位与校准：
> 临时工具正常启动游戏、切三个光照模式各截一帧，统计平均亮度 /
> 95 分位亮度 / 过曝像素占比（>0.95），调完即删。

**实测基线与修复后**：

| 模式 | 修复前 mean / 过曝占比 | 修复后 mean / 过曝占比 |
|---|---|---|
| 白天 | 0.790 / **46.4%** | 0.569 / 1.3% |
| 黄昏 | 0.644 / 11.9% | 0.511 / 4.4% |
| 夜晚 | 0.149 / 0.1% | 0.090 / 0.1%（观感正常） |

**根因**（三个来源叠加）：
1. **光照单位没有换算**：LOOK 表的数值是 Babylon 的光照单位，太阳 3.0 +
   半球环境 ×4 直接搬进 Godot，白天总入光量是合理值的 ~2 倍；
2. **ACES 曝光沿用原版数值**（1.08），把超标的入光原样顶到白；
3. **IBL 反射过强**：HDR 全景能量 0.78 让路面/幕墙把亮天空镜像回来；
   日落低角度时太阳镜面高光再把路面糊成白斑。

**修复**（`lighting_director.gd`、`sky_system.gd`）：
- 新增 `SUN_ENERGY_SCALE = 0.55`、`AMBIENT_ENERGY_SCALE = 2.2`（原 ×4），
  在 `apply_mode` 里统一换算 Babylon → Godot 光照单位；
- LOOK 表白天曝光 1.08 → 0.74、黄昏 1.00 → 0.78（夜晚 0.83 不动，实测本就正常）；
- `sun.light_specular` 0.5 → 0.35，压掉日落路面的镜面白斑；
- `ENV_INTENSITY`（HDR 全景能量 = IBL 反射强度）白天 0.78 → 0.45、
  黄昏 0.40 → 0.32（夜晚不动）。

**顺带**：图像设置页（F10）新增「曝光补偿」行（-30% ~ +30% 五档），
乘在 `tonemap_exposure` 上，即时生效并入存档——嫌暗/嫌亮可自行微调
（`graphics_quality.exposure_scale()`，白名单键 `exposure`）。

**验证**：曝光补偿全链路断言通过（0.74 → ×0.7/×1.3 精确生效并入档）；
900 帧常规空跑无错误。校准用截图与工具已删除。

### 26. 图像设置页（CS2 风格，F10）

> 2026-09-22。替换旧版只读的"画质设置"面板（`toggle_graphics` / `cycle_quality`
> 从未被任何按键调用过，`refresh_graphics` 只展示档位参数不能改）。

**交互**：`F10` 打开（打开即暂停、关闭即恢复），`↑↓` 选择行、`←→` 调整选项、
`F10 / Esc` 关闭。所有选项即时生效并自动落盘（`shenchengji-graphics-quality-v1.json`）。

**选项**（`PanelsUI.SETTINGS_ROWS`）：

| 行 | 选项 |
|---|---|
| 画质预设 | 低 / 中 / 高（切换即重置高级选项为该档默认，CS2 同款行为） |
| 渲染缩放 | 50%–150% 七档（`Viewport.scaling_3d_scale`，手动值优先于档位自动计算） |
| 抗锯齿 | 关 / FXAA（`Viewport.screen_space_aa`，运行时立即生效） |
| 阴影 | 关 / 低 (1024, 2-split) / 高 (2048, 4-split) |
| 环境光遮蔽 | 关 / 开（SSAO） |
| 泛光 | 关 / 开（Bloom） |
| 夜间路灯数量 | 8 / 16 / 24 盏（灯光池） |
| 植被密度 | 低 4000 / 中 9000 / 高 16000（林冠实例预算） |
| 帧率显示 | 关 / 开（与 `P` 键联动同步） |

**实现**：
- `GraphicsQuality` 新增 `overrides` 覆盖层：玩家手动改过的项优先于档位默认值；
  `effective()` 合并后经 `apply_to_world()` 广播到各子系统；
  `apply_aa()` / `apply_resolution_scale()` 直接在视口级生效。
- `SaveSystem` 画质存档升级 v2（tier + overrides，白名单过滤键与类型，
  兼容读取 v1）。**注意**：SaveSystem 不得反向引用 GraphicsQuality ——
  autoload 循环引用会让两边都报 "Identifier not declared" 编译失败；
  依赖必须单向（GraphicsQuality → SaveSystem），且 SaveSystem 排在前面。
- `LightingDirector.set_quality` 消费 `shadows` / `lamps` / `ao` / `bloom` 键；
  `ScenerySystem.set_quality` 消费 `tree_budget`；`GameHUD` 新增
  `set_fps_visible()` / `is_fps_visible()`。
- `main_game._on_key`：设置页打开时接管全部按键（吞掉驾驶键防误触），
  `KEY_P` 切帧率显示时同步写 overrides。

**验证**（临时驱动场景，跑完已删除）：打开/导航/逐项调整/预设重置/Esc 关闭/
存档往返/延迟复核全部断言通过，exit=0；900 帧常规空跑无错误。

### 27. 小地图溢出未裁剪（P2）

> 2026-09-22。用户反馈：小地图超出方框的部分没有被裁掉，直接显示在方框外。

**文件**：`scripts/ui/map_ui.gd`

**根因**：`_draw_minimap` 直接画在 `minimap` 控件上，代码注释写着"裁剪到内框"
但实际**没有任何裁剪**。Godot 的 CanvasItem 绘制默认不裁剪到控件矩形：
- 道路折线只有 ±60px 的宽松顶点过滤，线段仍可伸出方框 60px；
- 水域 / 绿地多边形（`_fill_ring`）**完全没有顶点过滤**，一个大水面的
  包围盒碰到视野就把整个多边形画出去，溢出到方框外的屏幕上。

**修复**：控件结构改为 `minimap_holder`（开启 `clip_contents = true`）
→ `minimap`（FULL_RECT 子控件，实际绘制）。注意 `clip_contents` 只裁剪
**子控件**的绘制、不裁控件自己的 `_draw` 输出，所以必须套一层父容器。
重绘节流、绘制逻辑、坐标映射均不变；实机截图确认方框边缘干净，600 帧
空跑无错误。

### 28. 自动驾驶不可用（P1：无触发入口 + 游标卡死绕圈 + 目的地选错点）

> 2026-09-22。用户反馈自动驾驶功能坏了。排查发现**三层问题叠加**，
> 用无头闭环测试（CarDrive + Autopilot 真实模拟开 8.6km）逐层验证。

**① 触发入口从未移植**：`main_game.start_autopilot()` 在全仓库**没有任何
调用点**（与第 9 条"骑手从未生成"同型）。原版从大地图选目的地触发
（city-map.ts onAutoDrive → world.startAutoDrive）。移植版大地图只能拖动
缩放，不能选目的地。

**② 目的地用错了坐标**：大地图选目的地必须用地标的 **`arrival`**（道路上
的到达点）。地标 x/z 是建筑中心，实测 44 个里 **10 个**的中心落在街区/
园区内部的孤立路网岛上，`graph.route()` 返回空路线、自动驾驶静默失败。
用 arrival 则 44/44 全部可达。

**③ 路点游标会卡死（移植失真，最严重）**：原版用**车在折线段上的投影**
推进游标（窗口 `cursor-3 .. cursor+25` 内找最近段）；移植版写成了
"距当前路点 <6m 才 _index+1"。车以巡航速度靠近路点时最小转弯半径
>6m，永远进不了 6m 圈 → 游标卡死 → 目标点落到身后 → **车在原地无限
绕圈**（闭环实测 400s 只挪 50m，其余时间都在打转）。

**修复**：
- `map_ui.gd`：大地图左键点选地标（区分点击/拖动，命中半径 26px）→
  发出 `destination_picked` 信号；绘制目的地标记与自动驾驶路线（浅绿）；
  底部提示更新。路线目标使用 `arrival`。
- `main_game.gd`：接信号 → 模式守卫（坦克/步行/观景提示，轿车启动）→
  `start_autopilot`，选完自动关大地图；**WASD/方向键随时接管**（原版
  "manual-takeover"）；到达/受阻 toast（每次启动只报一次）；下车、进
  观景、回正路面时取消自动驾驶并清除目的地标记；规划失败给出提示。
- `autopilot.gd`：游标改为**投影式**（`_advance_cursor` + `_proj`，窗口
  内最近段），前瞻距离从投影点沿折线累计；`_rollout_score` 的贴路线
  扫描从"扫到路线末尾"（长路线 3×30×500 次距离/帧）收窄为原版同款
  28 段窗口 —— 顺带修掉自动驾驶长路线下的每帧开销。

**闭环验证**：出生点 → 8.6km 外地标，442 秒到达（平均 ~70km/h），
直路巡航 23.3m/s、弯道/次干道自动降速（11.8–19.5m/s）、末段减速入位，
全程无绕圈、无卡死。600 帧常规空跑无错误。



### 0-3. 车模的前向轴被假定错了

`README` 与全部代码都假定「绝大多数资产前向 = +Z」，一律用
`node_yaw(yaw, PLUS_Z)`（即 `rotation.y = PI − yaw`）。**实测这个假定对车是错的。**

判据是各零件几何包围盒中心在模型局部坐标里的 Z（不是节点原点，节点原点全是 0）：

| 资产 | 判据 | 实际前向 |
|---|---|---|
| `car.glb` | 前轮 `wheel_lf/rf_rubber` Z=−1.43 ／ 后轮 `wheel_lr/rr_rubber` Z=+1.78 | **−Z** |
| `traffic-car.glb` | 前灯 `car_led` Z=−2.24 ／ 尾灯 `car_redled` Z=+1.62 | **−Z** |
| `floatplane.glb` | 螺旋桨 Z=−4.19，座舱玻璃 Z=−0.52（桨在座舱前方 3.7 m，拉进式） | **−Z** |
| `tank.glb` | 炮管 `tank_barrel_tube` Z=+3.54 ／ 炮塔座圈 Z=−0.90 | **+Z**（假定正确） |

**修复**：
- `main_game._place_car_model()` 与 `TrafficSystem` 改用 `ModelForward.MINUS_Z`；
- `_step_aircraft()` 构基时 z 轴取 `−fwd`（等价于绕 up 转 180°，
  `x_axis = y_axis × z_axis` 会自动跟着翻，保持右手系）；
- 坦克维持 `PLUS_Z`（炮管在 +Z，本来就是对的）。

### 0-4. 车灯装反了

**文件**：`scripts/vehicles/vehicle_lights.gd`

按 +Z 前向写的偏移：前灯在 `z = +2.25`（实际是车尾），尾灯在 `z = −2.2`（实际是车头）。

**修复**：前灯 `−2.25`、尾灯 `+2.2`，并抽出 `TAIL_OFFSET` 常量。
（`SpotLight3D` 默认沿自身 −Z 发光，车头也在 −Z，所以不需要旋转。）

### 验证

```
轿车 行进方向 = (-0.989, 0.000, 0.149)
轿车 车头指向 = (-0.989, -0.000, 0.149)
dot(行进, 车头) = +1.0000      ← 修复前是 −1.0000（倒着开）
前灯 dot = +2.250（在前）／尾灯 dot = −2.200（在后）
坦克(数学) dot(行进, 模型+Z即炮管向) = +1.0000
自检 PLUS_Z = +1.0000 ／ MINUS_Z = +1.0000   ← node_yaw 本身没错，错的是轴的假定
```

`README` 里「绝大多数资产是 +Z」那张表已按实测结果改正，并附上判定方法，
避免以后再凭猜。

---

## 零、画面类致命缺陷（P0）：整个画面只剩天空

> 这一组是在「启动正常、不卡了」之后才暴露出来的，排在前面是因为它最影响可玩性。

### 0-1. 天空球关掉了深度测试，把整座城市盖住了

**文件**：`shaders/sky.gdshader`、`shaders/night_sky.gdshader`

```glsl
render_mode unshaded, cull_disabled, depth_draw_never, depth_test_disabled, fog_disabled;
```

原注释写的是「depth_test_disabled + depth_draw_never 保证永远在最后方」，**这是反的**。

`depth_test_disabled` 意味着天空球的片元**不做深度测试、一律通过**；而不透明队列默认
由近及远排序，天空球距离 4000 m，排在城市、车辆之后绘制 —— 于是它把已经画好的
一切全部覆盖掉。表现就是「画面只剩天空，看不到车也看不到城市」。

**修复**：去掉 `depth_test_disabled`，保留 `depth_draw_never`。
这样天空只在**还没有任何东西**的像素上着色，先画后画都安全；
远处山体（>4000 m）也能正常压在天空之上。

```glsl
render_mode unshaded, cull_disabled, depth_draw_never, fog_disabled;
```

### 0-2. 雾密度被放大了 100 倍

**文件**：`scripts/world/lighting_director.gd:184`

```gdscript
env.fog_density = float(look["fog_density"]) * 100.0
```

原版是 Babylon `FOGMODE_EXP2`，density `0.00011` → 50% 雾约在 **7570 m**
（城市对角就是这个量级，雾只是略微压暗远景）。
而 Godot 的 exponential 雾用同一个数值，50% 雾约在 6.3~7.6 km —— **两种解释都对得上**。

乘 100 之后，50% 雾距离缩到 **约 76 m**：百米开外整座城市糊成一片雾色，
配合 0-1 的天空 bug，画面就彻底成了「天空色」。

**修复**：去掉 `* 100.0`，直接用原版数值。

### 排查过程（可复用）

无法在无头环境截图，所以写了个只读数据的探针，打印相机 / 车模 / 分块的真实状态：

```
相机全局位置 = (-2655.5, 2.25, 860.9)   current=true  near=0.75  far=18000
车模 pos    = (-2664.2, 0.02, 862.2)    visible=true  scale=(1,1,1)  26 个 mesh
分块        总数=1647  可见=465  离相机 700m 内可见=44
最近可见块  block_-5_-2_office  离相机=148.3m
```

相机确实在车后 8.8 m 并正对车辆，车模尺寸正常（轮胎 AABB 直径 0.88 m），
周围有 44 个可见分块 —— **几何、相机、模型全部正确**，问题只可能出在绘制顺序/着色，
于是锁定天空球的 `depth_test_disabled`。

顺带排除了两个可疑点：分块索引的 z 就是「北」（最近块真实世界坐标与索引一致，
不存在镜像错位）；相机 `cull_mask = 1048575`，没有漏层。

---

## 一、致命缺陷（P0）：启动即死循环，内存耗尽

### 1. `HeightField.load_relief()` 的 while 循环漏了自增

**文件**：`scripts/world/height_field.gd:231`

```gdscript
var t := 0
while t + 2 < icount:
    ...
    _relief_tris.append([a, b, c])   # 每圈都在追加
    ...
# ← 这里缺 t += 3
```

**后果**：循环条件永远成立，每圈往 `_relief_tris` 追加三角形，直到内存耗尽。
这正是「内存直线飙升、永远加载不完、最后 exit=1」的**根本原因** —— 不是数据集太大。

实测证据（修复前）：

```
ERROR: Parameter "mem" is null.  at: realloc_static (core/os/memory.cpp:161)
       [0] load_relief (res://scripts/world/height_field.gd:256)
```

单次运行就刷出 1 MB 以上的重复报错。

**修复**：补 `t += 3`。

**实测对比**：

| 阶段 | 修复前 | 修复后 |
|---|---|---|
| `HeightField.load_all` | 死循环，永不返回 | **91 ms** |
| `CityCollision.build` | 从未跑到 | **166 ms** |
| `RoadGraph.build` | 从未跑到 | **45 ms** |
| `blocked()` × 200 | 从未跑到 | **10 ms** |
| 城市构建 | 永远停在 15% | **frame≈150 完成，ready=true** |
| 内存 | 持续上涨直到 OOM | **稳定在 1064 MB，不再增长** |
| 退出码 | `exit=1` | **`exit=0`** |

> 附带说明：该文件里原本有一段注释把 `realloc_static` 报错归咎于
> 「Dictionary 下标取出的是 COW 副本」，是误判。真正原因就是这个死循环。
> 注释已更正，避免后人照着错误结论去改。

---

## 二、崩溃类（P0）

### 2. `ScenerySystem.diagnostics()` 强转类型错误，调用必崩

**文件**：`scripts/world/scenery_system.gd:355`

`_canopy_nodes` 里存的是 `MultiMeshInstance3D`，代码却写 `as MeshInstance3D`。
两者是 `GeometryInstance3D` 下的**兄弟类**，强转恒为 `null`，紧接着 `.multimesh` 就是空引用崩溃。
同文件第 190 行已经用对了类型，只有 diagnostics 这一处错。

**修复**：改 `as MultiMeshInstance3D` 并加空值守卫。

---

## 三、行为错误（P1）

### 3. 追车相机装到了车头**前方**

**文件**：`scripts/vehicles/chase_camera.gd:106`

`DRIVE_VIEWS` 的 `f = -8.6` 本身已经表示「沿车头方向 −8.6m（即车后）」，
代码却又乘了一次 `-f`，负负得正 → 相机落在车头前方。
同文件 `follow_walk` / `follow_aircraft` 都用「减前向」表示后方，可证此处写反。

**修复**：`focus + dir * f`。

### 4. 雨天路面材质停不下来，且越下越黑

**文件**：`scripts/world/weather_system.gd:192`

`set_raining()` 只在 `raining == true` 时调 `_apply_wet_materials()`，
函数里的 `else: 恢复` 分支永远不会执行。更糟的是它每次读「当前色」再乘 0.86，
反复下雨会按 0.86ⁿ 累积变黑。另有 `_wet_materials` 只 append 从不读取，随下雨次数无限增长。

**修复**：首次调用时把原始 albedo / roughness 拍进 `_dry_cache`（按资源实例去重，
因为 GLB 材质是共享的），之后雨天套用、雨停还原；删掉无用的 `_wet_materials`。

### 5. 立面瓦片加载失败后**每帧无限重试**

**文件**：`scripts/world/facade_streamer.gd`

失败后没有重置 `_retry_timer`，而选点逻辑是「`_retry_timer <= 0` 就不再拦截失败瓦片」，
于是缺失的瓦片每帧重新排队、每帧一条 `push_warning`，永不收敛。

**修复**：新增 `FAILED_RETRY_DELAY = 4.0` 与 `MAX_ATTEMPTS = 3`；失败后置重试计时，
连续失败 3 次把瓦片标记为 `dead` 永久跳过。

### 6. 程序化音频左右声道各算一次采样

**文件**：`scripts/core/procedural_audio.gd:134`

`push_frame(Vector2(_sample(), _sample()))` —— `_sample()` 内部会推进 `_bgm_sample`
与各相位，调两次等于每帧推进两个采样点：BGM 实际以 144 BPM 播放（设计值 72），
且左右声道取到的是两条互不相关的噪声。

**修复**：只算一次，左右声道共用。

### 7. 生涯系统的 3 条对话永远触发不了

**文件**：`scripts/life/career_system.gd:63`

`DIALOGUES` 引用 `daily-3 / office-3 / worker-3`，但 `CONTRACTS` 每个身份只有
`*-1 / *-2` 两单，`*-3` 根本不存在 → 6 条对话里 3 条是死数据。

**修复**：改为 `*-2`，与合约表对齐，注释也一并更正。

### 8. 调试跳变检测是死代码

**文件**：`scripts/life/story_system.gd:50`、`scripts/life/rides.gd:40`

`story.active` 与 `rides.debugged` 在全仓库**从未被置过 true**，
导致 `note_position()` 的跳变计数、`step()` 的「跳变后本单不可结算」全部失效。

**修复**：
- `story.active`：玩家真正推进剧情时置位，剧情完结时清除；
- `rides.debugged`：`step()` 里按单帧位移 > 45m 判定；接新单时清除。

### 9. 骑手 NPC 从未生成

**文件**：`scripts/world/ebike_system.gd:174`

`spawn_riders()` 全仓库没有任何调用点，`riders` 恒为空数组。

**修复**：`setup()` 里按出生点调用。
（注意：本移植未提供骑手模型，所以这里是「状态正确」而非「可见 NPC」，
渲染实体仍待接入。）

### 10. 建筑索引在过滤后失效

**文件**：`scripts/core/city_data.gd:160`

`building_index` 在 `_apply_landmark_exclusions()` 过滤**之前**按下标建立，
过滤后下标整体前移，索引会指向错误的建筑。当前无人读取所以没爆，属于埋雷。

**修复**：过滤后重建索引。

### 11. 立面流式初始化围着地图原点而不是出生点

**文件**：`scripts/world/city_world.gd:271`

`_plan_facades()` 传的 `_focus_data` 此时还是 `(0,0)`（`set_focus()` 要等主循环才调），
导致初始瓦片围着地图原点加载，玩家落地时周围没有立面。

**修复**：`_focus_data` 为零时回退到 `CityData.spawn_pos`。

### 12. `rides` 的 arrival 字段对 Variant 直接下标

**文件**：`scripts/life/rides.gd:62`

`s.get("arrival", [0,0])[0]` —— `get()` 返回 Variant，一旦 `arrival` 存的是
`Vector2`（Godot 4 的 Vector2 不支持 `[]` 下标）就会运行时报错。

**修复**：新增 `_arrival_of()` 统一处理 Array / Vector2。

---

## 四、性能（P2）

### 13. 天空 IBL 每次切模式/切雨都重建

**文件**：`scripts/world/sky_system.gd:137`

每次调用都 `new Sky()` + `new PanoramaSkyMaterial()` 再赋给 `environment.sky`，
换掉 Sky 对象会触发辐照度立方体贴图重算，而 `set_raining()` / `apply_mode()` 会频繁调用。

**修复**：缓存复用，只改 `panorama` 与 `energy_multiplier`。

### 14. HUD 片区名每次全量扫描地标表

**文件**：`scripts/ui/game_hud.gd:270`

HUD 以 ~8.3 Hz 刷新，`_area_name` 每次遍历全部地标求最近，无缓存。

**修复**：结果缓存，移动不超过 150m 就复用。

---

## 五、验证结果

修复后完整运行（`--headless`，900 帧）：

- 城市构建完成，`session_ready = true`
- 无脚本编译错误、无运行时报错
- 内存从 83 MB 加载至 **1064 MB 后持平**（不再增长）
- 退出码 `exit=0`
- 分块索引 1647 个、林冠 40927 棵、电摩 21110 辆、路网 42829 节点，均正常装载

剩余两条报错均为 headless 特有、不影响实机：
- `Parameter "t" is null`（dummy 渲染器无法创建纹理）
- `1 RID allocations ... DummyTexture leaked at exit`（dummy 渲染器退出时的清理告警）

---

## 六、尚未处理（需要资产管线改造，不在本次范围）

**全城常驻内存约 1 GB，分块剔除只切 `visible` 不释放。**

`CityWorld._cull_chunks()` 只做 `node.visible = should_show`，`blocks` /
`_chunk_index` / `landmark_nodes` 长期持有全部节点引用，`queue_free()` 只出现在
立面瓦片卸载和地标替换两处。所以 700m 可见半径只省绘制、不省内存。

真正的流式需要把 `buildings.glb`（266 MB）/ `roads.glb`（88 MB）在转换阶段按
640m 分块拆成独立 GLB（数据里已有 `block_i_j` / `roads_i_j` 命名可直接切分），
运行时再按块加载/卸载。这是 `tools/convert_glb.mjs` 侧的改造，会重新生成 `data/`，
风险与工作量都远大于本次代码修复，故未擅自进行。
